# DECISIONS.md — 未寫明細節嘅判斷記錄

規格來源:唯一 prompt(2026-08-05)。以下係規格冇寫明、由實作自行決定嘅事項。

## 專案層面
- **遊戲名**:規格冇提供名稱 → 定名「骰林 Dice Grove」。
- **Godot 版本**:機器上有 Godot 4.7.1(規格要求 4.3+),直接使用;`project.godot` features 標 4.3 以保兼容。
- **Renderer**:純 2D 手機遊戲 → `gl_compatibility`(中階手機最穩定)。
- **參考圖資料夾**:實際資料夾名係 `Art reference/`(唔係規格寫嘅 `art_reference/`),沿用現有資料夾。
- **版本控制**:自行 `git init` 方便每個 Phase checkpoint(純本地)。
- **UI 建構方式**:場景以最小 .tscn + GDScript 程序化建構 Control 樹,方便統一風格同大規模迭代。
- **音效**:用 GDScript 於啟動時程序生成 PCM(AudioStreamWAV in-memory),唔另外出 .wav 檔(規格允許 Python 或 GDScript 程序生成)。

## 數據層面
- **共用池數目**:規格標題寫「52 個」但實際列出 54 個面(攻21/防9/治7/資源控場11/特殊6)→ 以列表為準,全部 54 個實裝。
- **百年荊棘 Elder Briar**:「全隊荊棘3+格擋3」→ 解讀為全隊荊棘+3、自身格擋3(格擋冇寫「全隊」)。
- **殘心 Zanshin**:「下回合自身攻擊面+2」→ buff 喺下一回合全程有效,該回合結束後消失;與居合被動加成疊加。
- **鷹眼/標記(易傷)**:目標係敵人單體。
- **弱化 Weaken**:施加喺單位身上,影響該單位「本回合」所有骰面數值(攻/擋/治數值,最低0);對玩家施放嘅弱化影響玩家該英雄。敵方弱化玩家時,效果影響英雄本回合骰面。
- **易傷 Expose**:持續到回合結束。
- **暈眩 Stun**:即時取消一粒指定嘅敵方已擲未執行骰(玩家選目標骰);時停可選兩粒不同敵骰。對 boss 預告技(怒濤連擊/滅世咒)可用暈眩取消,做法係將預告視為一個可選嘅「虛擬骰」目標。
- **治療目標**:可選任何未倒下或倒下嘅隊友(倒下=復活);治療面預設單體目標任意隊友(含自己)。
- **洞察 Insight**:使用後本回合重擲+1,目標=無(self resource)。
  (原本 +2,2026-08-05 削弱;數值只寫喺 `faces.json`,tooltip 中英文、骰面數字、
  圖鑑同商店摘要全部由該值即時衍生,所以冇第二處文案要改。)
- **迴響水晶 echo**:「下一個使用嘅面效果+2」= 下一面嘅主數值(攻/擋/治/法力)+2;狀態類(暈眩/易傷)唔受影響。
- **賭命 Gambit**:攻擊值用 seeded RNG 均勻 1-12,喺分配使用時擲(undo 會回滾 RNG state)。
- **奪骰 Die Theft**:選一粒敵方已擲未執行嘅骰面,立即以使用者身份對敵方發動(攻擊面→由使用者揀目標;格擋/治療→施喺使用者身上);該敵骰被消耗。
- **敵方 Curse 詛咒**:玩家隨機一個(未 curse 嘅)骰面本場戰鬥變空白面。
- **偷骰 Steal**:唔會偷倒下英雄;歸還喺下回合擲骰階段。
- **Wild 百搭**:複製隊伍本回合任何一粒已擲骰面(含已用、含鎖定),以使用者身份結算;唔可以複製另一個百搭。咒文面經百搭複製一樣要俾法力。
- **敵人 AI 目標**:敵人攻擊目標喺結算時先決定(隨機/嘲諷),intent 顯示唔標明目標(單體攻擊顯示數值)。
- **精英詞綴**:狂暴=所有攻擊面+2;石膚=每回合開始格擋3;劇毒=攻擊面附中毒1。精英 HP=T級HP×1.5(向下取整)。
- **XP門檻**:Lv2=4/Lv3=10/Lv4=18/Lv5=28 為累積XP。

## UI / 流程層面
- **重擲時機**:規格分「重擲階段→分配階段」;實作合併為一個玩家階段——只要仲有重擲次數、骰未用,可以隨時鎖骰/重擲(嚴格更靈活,唔改變次數資源)。已用骰唔會被重擲。
- **敵人點擊目標**:規格要求互動元素喺下方60%,但點選敵人目標必須掂敵人卡(上方40%);屬必要例外。
- **Icon 字形**:⚔☠ 等 Unicode 符號喺 Windows 預設字型缺字 → 骰面/狀態/意圖 icon 用單個漢字(攻/防/療/毒/燒/弱/破/暈/縛/咒…)+ category 色。之後打磨階段可換自繪向量 icon。
- **戰鬥中斷存檔**:run 存檔以節點為粒度(規格如此);戰鬥中途熄 app,重開後由該場戰鬥開頭重打。
- **敵方施加嘅弱化/易傷**:敵人喺回合尾先行動,施加喺英雄身上嘅弱化/易傷延續到下一回合結束先清(否則永冇作用)。
- **玩家 undo 範圍**:骰面使用同藥水一齊入 snapshot;undo 會連藥水一齊回滾(完整回滾保證)。重擲唔可以 undo(防 reroll scumming)。
- **B1/B6 預告技**:以「虛擬骰」形式出現喺可暈眩目標清單,暈佢=取消預告。
- **B3 換階段**:階段轉換時清晒佢身上狀態,並失去本回合剩餘骰。
- **敵人 charge(蓄力)**:只加落攻擊面。
- **Thorns 荊棘**:戰鬥內持續疊加,唔隨回合衰減(配合 Bramble 被動同 R06)。
- **狀態附加時機**:攻擊附帶嘅中毒/灼燒,即使傷害被格擋完全吸收都照施加。
- **吸血結算**:以目標實際 HP 損失(扣格擋後)總和計 50% 向下取整;AOE 吸血計全部目標總和。

## Phase 6 補充
- **平衡改動**:規格授權「平衡迭代除外,且要記錄」——所有數值改動記於 BALANCE.md(10 次迭代,達標)。
- **角色美術**:全部以 `_draw` 程序繪製(Polygon/圓形拼合、#2b2b2b 4px 描邊),參考圖只取輪廓/配色;22 隻角色(6英雄+10小怪+6boss)各有獨立造型;2-frame stepped idle 浮動 + 攻擊 lunge tween。
- **音樂**:背景音樂暫缺(規格允許);9 種音效程序合成。
- **模擬器入口**:`godot --headless --path . -- --sim N [seed]`,輸出各章完成率/平均回合/承傷/死亡分佈。
- **效能**:浮動數字用 object pool;PawnArt 只喺 idle frame 轉變時 redraw;UI 全部 Control 節點、無每幀重建。
- **事件 V04 犧牲**:每人 -4 HP 以 1 HP 為底(事件唔會直接殺死英雄)。
- **骰之泉重鑄**:S/C→R、R/U/E→E(E 內重抽)。
- **V06 繞路**:跳過下一排 → 可自由揀再下一排任何節點。

---

# 第二輪(雙骰制)判斷記錄 — 2026-08-05

## 骰面數據
- **B骰面重用**:規格列出嘅 B 骰面之中,有 4 個同現有 A 骰/解鎖面「名同效果完全一樣」
  → 直接重用同一個 face id,唔另開重複資料:`moss_pierce`(貫穿箭)、`moss_hawkeye`(鷹眼)、
  `lily_channel`(聚能)、`lily_touch`(治癒之觸)。另外 `ember_daze`(迷眩)、
  `ember_vigil`(守望)亦重用——後者規格 B 骰寫「格擋3」而現有 A 骰面係「格擋4」,
  同名同效果兩張卡會令玩家混淆,取現有值。其餘 30 個 B 骰面全部新開 id(前綴 `<hero>b_`)。
- **湧流 Surge「法力+1+治療2」**:規格冇寫治療目標 → 定為單體隊友(target: ally),
  法力係無目標資源,一次結算兩者。
- **荊叢 Thicket「全隊荊棘1+格擋2」**:格擋冇寫「全隊」→ 全隊荊棘+1、自身格擋2
  (同第一輪「百年荊棘」嘅解讀一致)。
- **悶燒 Smolder「全體灼燒1」**:新增純灼燒 AOE(無攻擊值)結算路徑,同現有純中毒
  AOE(孢子雲)對稱。
- **A骰名**:規格只命名 B 骰(技巧骰/武士道骰…)→ A 骰自行命名(狩獵骰/刀骰/潮汐骰/
  壁壘骰/暗影骰/荊棘骰),只喺圖鑑同換面畫面顯示。

## 資料結構
- **12 格 slot**:英雄嘅 `faces`/`face_mods`/`face_plus`/`face_extras` 由 6 格擴成 12 格,
  slot 0-5 = A骰、6-11 = B骰(`GameData.die_of_slot()` / `slot_range()`)。舊存檔經
  `GameData.migrate_hero()` 自動補上 B 骰同新陣列。
- **「+」記錄方式**:`face_plus[slot]` 數強化次數(顯示幾多個「+」),`face_mods[slot]` 係
  數值總加成。鍛造/事件強化兩者一齊 +1;成長 keyword 只加 `face_mods`,所以唔會出「+」。
  `RunState.forge_face()` 係唯一會加「+」嘅入口。
- **戰後 offer**:`gen_offers()` 唔再預先揀 slot(規格 C1 改為玩家自選),
  offer 變成 `{hero, face}`。

## 戰鬥規則
- **鎖定 vs 鎖出**:兩種「鎖」意思唔同,UI 用唔同 icon 分開——玩家自己 pin 住防重擲嘅骰
  用 📌,因為行動咗而被鎖住嘅另一粒骰用 🔒。
- **B4 偷骰**:偷嘅係「該英雄嘅回合」,兩粒骰一齊唔擲(維持第一輪「該英雄失去回合」嘅效果)。
- **敵人 cancel_die(消骰)**:同樣係令該英雄本回合唔可以行動(唔係淨係消一粒)。
- **百搭 Wild**:可以複製場上任何一粒骰,包括使用者自己另一粒骰(第一輪只可以複製其他英雄)。
  雙骰之下限制自己嘅骰冇道理,而且反正一個英雄一回合只行動一次。
- **居合上限**:規格寫 +8,`IAIDO_CAP` 6→8;條件由「粒骰未使用」改為「兩粒骰都未使用」,
  等同「該英雄本回合冇行動過」。

## 拖曳操作(規格 D1 未涵蓋嘅目標類型)
- **暈眩/奪骰(target: enemy_die)**:規格冇講點拖。做法係敵人卡上每個意圖 chip 都係
  獨立 drop zone;拖去 chip = 指定嗰粒骰,拖去敵人卡身(唔中 chip)= 自動揀嗰隻敵人
  攻擊值最高嘅未執行骰。時停(暈2)第二個目標自動揀場上次高嘅。
- **奪骰兩段式**:拖去敵骰 = 揀定要偷邊粒,之後高亮所有敵人等你揀攻擊目標(拖或點都得)。
- **百搭兩段式**:拖去另一粒骰 = 揀複製來源;如果複製到嘅面需要目標,轉為點選模式高亮目標。
- **自身效果(target: self)**:只可以拖去自己嗰欄(跟規格);全隊效果(team_*)可以拖去
  任何隊友欄;純資源(法力/洞察/重擲)先至用中央施放區。
- **保留點擊操作**:規格話「拖曳為主」,冇話要移除點擊 → 點骰選中 + 點高亮目標嘅舊流程
  完整保留,方便單手操作同 headless 測試。
- **手勢由邊個負責(2026-08-05 修 bug 後)**:`DieVisual` 淨係負責「認出一個 press
  變成 drag」,一過門檻就 `drag_started` 交俾 `screen_battle`,自己即刻停止追蹤;
  之後嘅移動同放手由 `screen_battle._input()` 處理。原因:拎起粒骰要 `_refresh()`
  重畫高亮,而 `_refresh_heroes()` 會 `queue_free()` 成行英雄欄——即係包住嗰粒
  正在被拖嘅骰。舊寫法將 `drag_moved`/`drag_ended` 掛喺骰身上,粒骰一死就再收唔到
  事件,結果幽靈定格、放手冇反應、`drag` 狀態永遠清唔到。喺 screen 層做仲順便
  越過所有會食事件嘅 overlay(tap button、tooltip 遮罩)。
- **座標空間統一用 canvas space**:所有 drop zone 都係 `get_global_rect()`(canvas
  座標),所以事件位置一律用 `get_global_transform() * event.position` 換算。
  **唔可以用 `event.global_position`**:佢係原始視窗像素、唔受 `canvas_items`
  拉伸影響,得喺啱啱好 720×1280 嗰個視窗先同 canvas 一致;而且觸控事件根本冇呢個屬性
  (舊寫法對觸控用 local 座標做拖曳門檻,令門檻永遠一 pixel 就觸發)。
- **一個 drag 只屬於一隻手指**:`drag.pointer` 記住撳落嗰陣嘅 touch index(滑鼠 = -1),
  第二隻手指喺第度撳唔會提早放低隻骰。
- **唔可以用嘅骰完全拖唔起**:`DieVisual.draggable` 同 `interactive` 分開——已用/被鎖/
  空白/法力不足嘅骰照舊可以點(出「唔得」音效)同長按睇 tooltip,但根本唔會離開枱面,
  而唔係拖起咗先發現冇效。

## UI
- **骰面名顯示**:雙語模式下兩行細字(中12px/英12px),單語模式一行 14px;
  淨顯示名(含「+」),效果文字維持喺長按 tooltip(跟規格 D2)。
- **骰仔尺寸**:Control 本體 76×76(≥ 規格要求 72px 觸控面積),立方體正面 68×68,
  頂/右側面同投影畫喺剩低嘅 8px 入面——即係整個 2.5D 立方體都喺自己 rect 內,
  唔會蓋到隔離嘅 widget。
- **英雄欄闊度**:4 欄要塞落 720px → 每欄 166px、panel 內邊距收窄到 4px、
  兩粒骰間距 6px(76×2+6=158 ≤ 158 內容闊度)。
- **目標高亮時嘅變暗**:唔合法目標變暗只套用喺英雄欄嘅頭像區,唔套用喺骰上——
  否則自己隊友嘅骰會睇落好似唔可以用。
- **圖鑑解鎖顆粒度**:小怪逐 tier 解鎖(打過 T2 先睇到 T2 數值),boss/詞綴整隻解鎖。
  骰面以 base_id 計「用過」,任何英雄用過都算全域見過(跟規格 E2)。

## 防 save-scum
- **戰鬥 seed**:進入戰鬥節點時即刻用 run rng 抽一個 `battle.seed`,連同敵人組合、
  章節、詞綴、opts 寫入 `run.battle` 並 `save_run()`,**先至**開始擲第一粒骰。
  戰鬥內所有隨機(擲骰、賭命、敵人選目標、召喚)全部行呢個 seed,所以中途熄 app
  再 continue 會完整重播同一場。戰鬥結束(勝或敗)清空 `run.battle`。
- **戰後隨機**:金幣/獎勵/遺物改為用 run rng(唔再沿用戰鬥 rng 嘅 state),
  避免戰鬥長短影響戰後掉落。
- **未防到嘅**:玩家喺同一場戰鬥入面可以改變自己嘅行動順序,咁樣後續擲骰結果會唔同
  (因為 rng 係共用序列)。呢個係「重玩策略」唔係「重刷骰」,規格冇要求防。

## 模擬器策略
- 每個英雄用同一套優先級(斬殺 > 消威脅 > 輸出 > 格擋 > 治療)同時評分兩粒骰,
  邊粒入到更高優先級嘅 bucket 就用邊粒;同 bucket 內比較預期傷害。
- 冇基礎重擲之下,重擲當成稀有資源:淨係喺「兩粒骰都評分 <2 嘅英雄有 2 個或以上」
  先使一次,而且會先 pin 住評分 ≥3 嘅骰。
- 戰後 offer:因為 slot 改為玩家自選,模擬器換走該英雄稀有度最低嗰格。

## 美術/UI 打磨輪(2026-08-06)

### 設計系統
- **所有數值集中喺 `scripts/ui/ui_theme.gd`**:spacing 只准 4/8/12/16/24/32、
  圓角 6/10/14/20、描邊 2/3/4/5、字級三級制(標題 60/38/28、正文 22/18、細字 15/12)。
  `UIKit` 淨係負責砌 widget,唔准再喺 screen 度打 magic number。
- **深色卡取代半透明白**:卡面由 `Color(1,1,1,0.10)` 改為實色 `UITheme.surface(chapter)`。
  半透明卡嘅對比度由背後嘅底色決定,同一個 widget 喺三個章節會有三種可讀性;
  實色 surface 之下 cream 字永遠 ≥11:1。
- **細徽章一律「深底 + 奶油字 + 亮色鑲邊」(`UIKit.chip`)**:飽和中間色調撐唔起
  4.5:1 嘅字(紅 3.1、藍 3.6、紫 3.9,無論用 cream 定 ink 都唔夠),所以
  `CAT_DEEP` 做底、真色做邊——色相照樣認得出,字又讀得到。
- **直接印喺章節天空底嘅字只准用 `CREAM`**:`CREAM_DARK` 喺第 1 章底色得 3.9:1。
  卡入面就冇呢個限制。呢條規則由 `tests/ui_smoke.gd::_t_contrast()` 守住。
- **雙語 chip 策略**:一個 chip 得一個漢字或者三個字母嘅位置,放唔到兩樣。
  所以雙語/純中模式用漢字(`UIKit.ICON`),純英模式轉 `UIKit.ICON_EN`
  (`ATK`/`BLK`/`PSN`…),入口統一係 `UIKit.glyph()`。

### PawnArt 排版
- **`PawnArt.EXTENT` 係量出嚟嘅,唔係估**:每隻造型手畫,實際佔用由 0.45×
  到 1.29× `body_h` 不等。用名義高度排版 = 細怪浮喺大卡中間、boss 帽爬上卡名。
  `tools/pawn_extents.gd` 逐隻 render 入透明 SubViewport 度 alpha bounding box,
  輸出可以直接貼返入 `PawnArt.EXTENT` 嘅 dict。**改咗任何 `_draw_*` 就要重跑一次。**
- 排版一律用 `PawnArt.fitted(kind, box)`:按兩軸取細嗰個縮放,填滿個框又唔爆框。

### 戰鬥版面
- 畫面分區寫死做常數(`ZONE_*`),敵人企喺 y=466 嘅地面線上(`SHRINK_END`),
  英雄欄置中(`SHRINK_CENTER`),操作 tray 釘死喺畫面底 244px。
  原本嘅版面係為更高嘅畫布度身訂造,喺 720×1280 剩返 35% 死位。
- **施放區改為常駐**:閒置 55% 透明,拖住無目標骰先著綠。一來填走中段死位,
  二來玩家未拖之前就見到有個落點,唔使靠試。
- **Boss 有尺寸下限**(art ≥210px、卡 ≥236px):敵人卡尺寸本身按場上數量縮,
  但 B6 帶兩隻小兵出場嗰陣 boss 就會縮到同小兵一樣大,冇晒氣勢。

### 浮動數字 bug(根因)
- `_do_use()` 嘅次序係 `_refresh()` → `_spawn_floats()`。`_refresh()` 會
  `queue_free()` 成行卡再起新嘅,而新卡喺同一 frame 未經 container sort,
  `get_global_rect()` 仲係 `(0,0,0,0)` → 數字飛去左上角。
- 修法唔係調轉次序(`_spawn_floats` 要讀 refresh 之後嘅 widget),而係
  **喺 `_refresh()` 開頭、free 之前影低錨點**;讀嘅時候用 `_laid_out()`
  (size>1 且 position≠0)判斷 live rect 信唔信得過,唔信就用快照,兩者都冇
  就用幾何 fallback。錨點仲會經 `_to_float_space()` 換入 `float_layer` 座標,
  所以 screen 被包喺有 transform 嘅父層(gallery exporter 就係)一樣準。

### 截圖工具
- `tools/gallery_export.gd` + `tools/gallery.sh <N>`:一個 command 影晒 29 個
  畫面 × 三語(戰鬥/圖鑑)× 720/540 = 84 張,輸出 `art_iterations/iter_N/`。
- 540 唔係另一套版面,而係將 720×1280 嘅畫布整個縮 0.75 掛喺 SubViewport 入面——
  同 project 嘅 `canvas_items`/`expand` 拉伸行為一致(兩個目標都係 9:16)。
- **一定要有真視窗**(`frame_post_draw` headless 唔會 fire)。

---

## 第二輪 UI/UX/美術(真人試玩回饋)

### 交互:單點永遠唔會用骰,拖曳要離開原位 56px
- **根因**:自身目標嘅骰本身就坐喺自己張英雄卡入面。舊嘅拖曳門檻係 10px,
  手指郁少少 → 起拖 → 放手 → `_hit_test()` 見到手指喺英雄卡上面 → 直接用咗。
  更衰嘅係英雄卡係 `PanelContainer`,直接掉個 tap Button 落去會被拉到成張卡
  咁大,連下面兩粒骰都冚住,所以「選取之後再點粒骰」= 用咗佢。
- **修法兩層**:
  1. `ScreenBattle.DRAG_ARM = 56.0` — 拖曳要離開按落去嗰點 56px(闊過一粒骰)
     先進入「可放置」狀態;未夠就放手 = 取消,即使手指啱啱好停喺合法目標上。
     `DieVisual/Die3D.DRAG_THRESHOLD` 保持細(12px)——粒骰一郁就要離手,
     真正決定使唔使得嘅係 arm 距離。
  2. 英雄卡嘅 tap 目標改為只蓋住 portrait:個 Button 放喺一個普通 `Control`
     入面(`Control` 唔會 lay out 佢啲仔),再 deferred 對位到 portrait 個 rect。
     骰嗰一橫行永遠屬於骰。
- **點解仲保留 tap 目標**:規格寫「使用骰唯一途徑:拖曳」,但單手玩同無障礙
  都靠 select→tap-target,headless 測試亦然。判斷:**點骰**永遠淨係選取/取消,
  **點目標**(敵人卡、意圖 chip、隊友 portrait、中央施放區)先會結算。玩家投訴嘅
  誤觸全部係前者。中央施放區因此新加咗一個 tap 按鈕(以前淨係可以拖)。
- **落點預覽**:armed 之後,手指下面嘅目標會放大 3.5% + 換成綠色粗框,離開就
  即刻還原(`_update_hover` / `_clear_hover` 記低原本嘅 scale 同 border)。
  Ghost 亦由半透明細粒變成實心大粒,俾手感知道「而家放手就落到」。
- 測試:`tests/drag_input_test.gd` 加咗 `_case_tap_selects_only`、
  `_case_short_drag_cancels`(30px,而且刻意停喺合法目標上)、`_case_armed_self`,
  三種 input mode 各跑一次。

### 真 3D 骰(`scripts/ui/die3d.gd`)
- 每粒骰 = 一個 `SubViewport` + 倒角立方體(6 個面 quad + 12 條邊 + 8 隻角,
  邊同角用純色 ink material,直接當咗成隻遊戲嘅粗描邊)。6 個面貼一張 3×2
  atlas,atlas 本身又係一個 `SubViewport`,入面擺 6 個程序畫嘅骰面 tile。
- **一定要 `own_world_3d = true`**:`SubViewport` 預設會借用父 viewport 個
  World3D。唔開嘅話 8 粒骰嘅 cube/camera/light 全部疊喺原點同一個世界度,
  render 出嚟一片黑加 z-fighting(第一次就係咁)。
- **唔可以用 `UPDATE_ONCE`**:atlas 自己都係 viewport,單 frame 更新有機會同
  atlas 同一 frame 發生,結果 cube 抽到一張空 texture 而且永遠唔會自己修正。
  改為「pump 3 frame」(`_render_once()`),之後即刻 `UPDATE_DISABLED`。
- **靜止零成本**:靜止時兩個 viewport 都停 render;擲骰/手指轉動先開
  `UPDATE_ALWAYS`。
- **朝向係量出嚟,唔係推導出嚟**。`rest_basis()` 用 `Basis(u, n, u×n)` 嘅逆
  將擲出面帶上頂;正確嘅 90° 站位、以及「印上去嘅面朝下方向沿 u 軸」呢兩件事
  都係靠 `tools/dice_bench.gd` 逐個 render 440px 大骰對出嚟嘅。
- **側面唔會扶正**:一粒立方體冇可能六面同時企直——換一個面上頂就會令其他面
  轉 90°/180°,真骰亦係咁。而且呢個 camera 角度側面本身已經被等角投影切斜,
  四個 90° 站位冇一個睇落係「直」。所以只有**頂面**(唯一載住資訊嗰面)會按
  camera 方位扶正(`_orient_atlas()`),側面順其自然。
- **長按 → 詳情浮層**,入面粒骰可以用手指自由撥轉(有慣性,鬆手緩慢回正),
  同時列晒 6 個面嘅 tile 同全部名詞解釋。

### 擲骰 stress 實測(結論:全程用 3D,唔使降級)
720×1280 真視窗、關 vsync、`gl_compatibility` renderer,`tools/dice_bench.gd`:

| 情況 | 平均 frame time | 最差 |
|---|---|---|
| 8 粒骰靜止 | **0.38 ms** | 1.09 ms |
| 8 粒骰同時擲 | **0.59 ms** | 1.45 ms |

16.7ms 預算入面用咗 3.5%。規格提過嘅降級方案(擲完烘焙成靜態快照、長按先開
3D)**冇需要做**,記錄喺此以免日後有人再提。

### 擲骰動畫
- 彈起 → 兩軸 tumble 2-3 圈 → 落地彈兩下(每彈一下旋轉衰減,`pow(1-t, 1.7)`)
  → 定格喺結果面 + squash。總長 0.68s,「快速動畫」開關縮到 0.30s。
- 每粒骰隨機 delay 0~0.15s(快速模式 0~0.06s),落地各自出一下木聲,音量遞減。
- **震屏封頂**:只有頭三粒落地會震,而且只震 2.5px。8 粒各震一次會變地震。
- 動畫全部做喺內層 `TextureRect` 上面,**唔郁 widget 本身個 rect**——drop zone
  同 hit test 都靠 `get_global_rect()`,粒骰識行嘅話會連 touch target 一齊搬走。

### 骰面顯示系統
- **`scripts/ui/glyphs.gd`**:35 個關鍵字各有一個手畫向量圖示(劍/盾/十字葉/
  水滴晶/毒滴/火/星暈/斷箭/裂靶/面具/刺/紅滴/雙箭/問號星/穿盾箭/扇形/三箭/
  芽/裂心/四葉/法陣/鎖鏈/紫霧/眼…)。**點解唔用字或者 emoji**:骰面要喺 76px、
  三種語言模式、六種底色下一眼睇得明。漢字縮到咁細會糊,emoji 睇平台 fallback
  字型面色,兩樣都做唔到「主要嘅讀取」。多邊形做得到。
- **`data/glossary.json` 係唯一來源**:原本喺 `strings.json` 嘅 `kw_*` 全部搬過嚟,
  再補齊以前完全冇解釋過嘅隱含機制——蓄勢(冥想/殘心)、居合、迴響、淨化、
  自療、每回合一次、空白面、以及敵人嗰邊嘅蓄力/反擊/嚎叫/召喚/消骰/吸取法力。
  `Glossary` 負責讀,`Data.kw_line()` 變成薄 wrapper。
- **`FaceTile`**:主效果 glyph + 大數字置中 + 最多 3 個次要 keyword 角標 + 面名。
  戰鬥骰面、換骰面介面、圖鑑、商店、詳情卡全部同一個組件。
- **`DetailCard`**:① 面名+稀有度+強化 ② 完整效果句(由已解析嘅 face dict 生成,
  所以鍛造 +1 同成長層數自動反映) ③ 目標 ④ 逐個名詞解釋。狀態圖示同敵人意圖
  chip 用同一套 glyph,長按開同一份 glossary。
- 教學加咗一步示範長按。

### 換骰面介面(`scripts/ui/face_swap.gd`)
- 12 個面全部 tile 化,分 A/B 兩組,每組旁邊有粒細 3D 骰。新面大大隻擺頂部,
  連完整效果句。換面動畫:舊 tile 飛走 → 新 tile 由下而上嵌入原位 → 粒骰快速
  spin + 閃白 → 先至 callback。
- 底部角色列有頭像同名(跟語言模式)。**四個角色都撳得**:唔係 offer 綁定嗰位
  會標「只可查看」,佢啲 tile 撳落去係開詳情卡而唔係選取。
- 商店、寶箱、獎勵、鍛造全部行同一個介面(`RunWidgets.pick_hero_face` /
  `pick_team_face` 變成薄 wrapper)。

### 森林氛圍(`scripts/ui/forest.gd`)
- 三層靜態景深:遠(山脊/樹冠剪影)、中(樹幹)、近(草叢 + 四角暗角)。
- **樹幹只企喺畫面左右兩邊**(`TRUNK_X` 全部喺 0.25 以外),高度封頂喺
  canopy→horizon 距離嘅 42%。第一版樹幹又高又企喺正中,由按鈕背後穿過去,
  睇落係雜訊唔係景深。
- **暗角只食四隻角**(三角形),中間版面區域完全冇改動 → `_t_contrast()` 量到嘅
  組合同以前一模一樣。
- 環境粒子:第 1 章螢火蟲、第 2 章落葉、第 3 章孢子,18 粒 pooling 循環再用,
  設定頁可以關(關咗連個 node 都唔會建)。
- 按鈕 = 木牌:木紋同木釘畫喺 label **底下**(先 add + `MOUSE_FILTER_IGNORE`),
  按落去去陰影 + 內容下沉 3px。面板四角加藤蔓捲鬚,畫喺 content margin 以外。
- 地圖:路徑改做二次曲線蜿蜒泥路 + 腳印虛線,兩旁散樹,每個節點企喺一個石圈
  空地上面。彎度由節點座標推導,唔用 rng,所以每次 render 都一樣。
- 圖鑑:每個角色嘅骰面由密集文字表改成 FaceTile grid,撳/長按開詳情卡。

### 寶箱去重(唯一掂規則層嘅改動)
- `RunState.gen_treasure()` 三選一唔會再出現重複 base_id:重複就重抽(上限 60 次),
  再唔夠就由排序好嘅 R+E pool 補齊,所以 pool 縮細只會變成「有幾多個唔同就出
  幾多個」,唔會死 loop。
- `tests/run_flow_test.gd::_t_treasure_distinct()` 固定 seed 掃 500 次。

## 第三輪試玩回饋(2026-08-06,純視覺/交互,冇掂 gameplay)

### 1. 主選單啲骰變成玩具(`scripts/ui/screen_menu.gd`)
- 森林地面嗰三粒骰由 `interactive = false` 改成 `enable_free_spin()`:撥一下就
  轉,有慣性,慢慢衰減,最後自己企返正。用返戰鬥/詳情卡同一個 `Die3D`——
  呢個係一隻講骰嘅遊戲,主選單就係玩家發現「骰係實物唔係 icon」嘅地方。
- **佢哋冇任何功能**,亦唔會影響任何按鈕(骰喺 y≈1110 以下,按鈕欄喺 452~800)。
  骰面隨便襯三個角色嘅起手骰,純裝飾。
- 加咗一行細字提示「撥一撥啲骰」,唔講嘅話冇人知撳得。
- **性能**:靜止時零成本——`Die3D` 只喺郁緊嗰陣 render,停低之後 viewport 瞓返。
  截圖驗證:靜止後 `is_processing() == false`、`render_target_update_mode ==
  UPDATE_DISABLED`,而畫面唔係灰版(見下面「最後一格」)。
- **手感參數維持原值**(`SPIN_DECAY = 1.6`):呢個係詳情卡本身已經調過嘅數,
  菜單骰係同一件玩具,冇理由兩處唔同。一次大力撥大約 coast 5 秒。

### 2. Offer 卡加角色頭像(`MiniPortrait`)
- 新組件 `scripts/ui/mini_portrait.gd`:圓底 + 粗描邊 + **裁現有立繪個頭**,
  唔係第二張圖。`PawnArt.HEAD` 記低每個設計個頭喺 140px 授權空間嘅中心同半徑,
  組件按呢個縮放同對位,所以頭像同棋子永遠係同一張畫。
- **點解用 `clip_children`**:Control 只有矩形 `clip_contents`,圓形裁切要靠
  `CLIP_CHILDREN_AND_DRAW`——父節點畫嗰個圓就係 mask。保險起見 `_Ring` 仲畫多
  一圈底色遮四隻角:`clip_children` 生效時佢自己都被裁走(睇唔到),唔生效時
  就頂上。
- 頭像唔做 idle bob:3px 上落喺 54px 嘅裁切入面唔似呼吸,似頭喺度抽筋。
- **接駁點喺共用組件** `RunWidgets.offer_card()`(新參數 `hero_id`)同
  `face_card()`,所以獎勵/商店/寶箱/事件全部行同一個卡形。獎勵畫面嗰行灰色
  角色名字幕已經刪走——頭像講咗嘅嘢唔使講兩次。
- **商店同寶箱實際上唔會出頭像,呢個係啱嘅**:`gen_shop()` / `gen_treasure()`
  只抽 `sp_*` 共用池,呢啲面唔屬於任何角色,而且買咗之後係玩家自己揀邊個收。
  卡上面畫個頭像就係講大話。機制照做:`face_card()` 冇 `hero_id` 就會 fallback
  去 `faces.json` 嗰個 `hero` 欄,將來只要有角色專屬面入到嗰啲池,頭像自動出。
- 三種語言模式各截一張:名字用 `heroes.json` 嘅 `short`(英文)避免
  「Moss the Frog Ranger」塞爆 80px 直欄;雙語模式上下兩行。

### 3. 長按詳情卡三個修正(`scripts/ui/detail_card.gd`、`die3d.gd`)
- **3a 灰版根因**:`show_die()` 係喺粒骰**入 tree 之前**就 `enable_free_spin()`,
  而舊 `_render_once()` 有一句 `if _throwing or _free: return`——即係 `_ready()`
  嗰下要影嘅第一格被自己拒絕咗,viewport 由頭到尾冇 render 過。撳一下 → 
  `_free_spin_input` 開 `UPDATE_ALWAYS` → 先至見到骰。
  **修法**:`_render_once()` 淨係避開 `_throwing`(擲骰本身已經 drive 緊
  `UPDATE_ALWAYS`),free-spin 唔再係拒絕理由。「等陣可能會撥佢」唔等於
  「而家唔好畫佢」。
- **3a 另一半:最後一格**。舊 `_process()` 喺定位完成嗰一幀就即刻
  `UPDATE_DISABLED`,即係最後嗰個 pose 未去到 texture 就熄咗,粒骰凍結喺
  差一格嘅角度。而家所有「熄」都要行經 `_pump`(新 `_sleep()` 統一守門,
  仲會檢查 `_turning` / `_spin`),定位完成只係設 `_pump = 2`。
- **檢查過所有用同一組件嘅位**:戰鬥長按卡、換骰面介面(`face_swap` 每組骰旁邊
  嗰粒細骰)、圖鑑/詳情卡三種形態,全部截圖確認。`face_swap` 嗰啲本身冇事
  (佢哋唔係 free-spin),但係同一個 `_render_once` 路徑,一齊覆蓋。
- **3b**:「擲出:XXX」由淨文字改成 `FaceTile`(64px、唔顯示面名)+ 文字。
  全隻遊戲最重要嗰一個面,以前係卡上面唯一冇圖嘅嘢。
- **3c**:卡右上角掛咗個木牌 ✕ 掣。放喺 `PanelContainer` 入面一個冇最小尺寸嘅
  `Control` 度——咁樣佢會鋪滿 content rect 但唔會改變 panel 量到嘅尺寸,個掣就
  可以錨定右上角而唔使 deferred 度位。個 ✕ 係**畫**出嚟唔係打字:內建字型未必
  有 U+2715,每張卡角落出個豆腐格唔值得冒險。點卡外面照舊可以閂。

### 測試
- `tests/ui_smoke.gd::_t_die_render()` 鎖死上面兩個 bug:用詳情卡嗰個順序
  (先 `enable_free_spin()` 後 `set_die()` 後入 tree)建粒骰,斷言入 tree 嗰刻
  viewport **唔係** `UPDATE_DISABLED`;pump 完之後斷言瞓返;然後撥一下,斷言
  會轉、會停、停低之後 viewport 熄咗、而且係停喺正面。
- `_process()` 係用固定 delta 手動叫嘅:headless 幀率無上限,用幀數計時等於量
  緊部機幾快。
- 截圖工具 `tools/fb_shots.gd`(要真窗口),輸出去 `final/fb/`。

---

# 第四輪:主題術語重整、書面語化、遺物重做(2026-08-06)

## 1. 「法力 Mana」→「靈息 Essence」
世界觀先行:角色唔係自帶魔力,而係向森林借。所以資源叫**靈息**,施法叫**靈術**。

- **顯示層改晒,內部 key 保持 `mana` / `spell`**。理由:`mana` 呢個字喺
  `faces.json`、`BattleCore`、存檔、事件、測試入面出現八十幾次,改 key 等於同時
  改規則層同存檔格式,而玩家一個字都睇唔到分別。術語係文案問題,唔係資料問題。
  單一真相仍然喺 `data/glossary.json` 嗰兩條 entry(`mana` / `spell`)——所有
  UI 文字經 `Glossary` 出,所以改一次就到處都改。
- 骰面改名:聚能 Channel→**引靈 Gather Essence**、大聚能 Deep Channel→**深引
  Deep Gather**、湧流 Surge→**靈泉 Essence Spring**;效果一個數字都冇改。
- Lily 被動 潮汐親和 → **森林親和 Sylvan Affinity**(`passive` key 由
  `tidal_affinity` 改 `sylvan_affinity`;呢個 key 冇任何代碼讀,純標籤)。
- B6「法力汲取」→「**靈息汲取** Essence Drain」。
- `UIKit.ICON` 嘅單字 token 由「力」改「靈」,英文 mode「MP」改「ESS」。
- **驗收**:全 repo grep「法力」「咒文」「Mana」「Spell」喺玩家可見字串度零殘留
  (內部 key 除外)。

### 藍色靈息條(`screen_battle.gd::_EssenceBar`)
- 由「✦ 3/10」一行字改成 **10 格刻度條**:填色格 = 現值,凹陷格 = 空位,
  數字細字疊喺條中間。一個「填滿再花掉」嘅資源應該睇落似個表,唔係似個計數器。
- **長按彈 glossary**(靈息 + 靈術兩條),同其他所有徽章一樣嘅習慣。
- **隱藏規則**:隊伍八粒骰九十六個面入面一個 `mana` / `spell` 面都冇,而且冇
  靈息水晶 / 森之心,就唔顯示。一條永遠郁唔到嘅條讀落係「壞咗」,唔係「未用」。
  每場戰鬥開頭重新判斷(骰面 run 中途換得)。

## 2. 廣東話 → 標準書面中文
- 掃咗 **755 條玩家可見中文字串**(`data/*.json` 嘅 `zh`/`d_zh`/`desc_zh`/
  `text_zh`/`passive_zh`/`gimmick_zh` 共 **584** 條,加 `scripts/**/*.gd` 入面
  嘅中文字面 **171** 條;統計已排除純註釋行)。
- 檢查方式係機器 + 人手兩層:先用粵語特徵字集(嘅唔咗喺畀哋嗰咁啲乜冇睇攞諗嚟揀)
  掃全部 `data/*.json` 同 `scripts/**/*.gd` 嘅非註釋行,**殘留 0 行**;再人手
  過一次全部 `strings.json`、`glossary.json`、`events.json`、`relics.json` 同
  教學文案,確認冇中英夾雜同書面/口語混用。
- **代碼註釋照樣冇改**(依規格),唯一例外係 `UIKit.mouse_passthrough()` 上面
  嗰段粵語註釋 —— 佢係全 repo 最後一段,順手翻埋做英文同其他註釋一致。
- 本文件(DECISIONS.md)係內部文件,保持原有粵語語氣。

## 3. 敵人行動逐個演出(`BattleCore` 分步 API + `screen_battle`)
- **引擎層**:`end_turn()` 拆成 `end_turn_begin()` / `peek_enemy_action()` /
  `enemy_step()` / `end_turn_finish()`。`end_turn()` 保留,內部就係跑晒佢哋——
  模擬器同所有 headless 測試一句都唔使改。
- **點解要 `peek`**:UI 要喺傷害落地**之前**知道邊隻敵人、邊粒骰、打邊個,先做到
  「亮起 → 意圖 chip 放大 → 飛去目標 → 先至扣血」。如果只有 `step()`,演出就
  只能事後補畫,同舊版一樣冇因果。
- 節奏:每個動作 `STEP_BEAT = 0.30s`,chip 飛行 0.20s **喺**節拍入面(唔係加落去)。
  「快速動畫」設定將所有拍子 ×0.5,同擲骰動畫用同一個開關。
- 受擊表現:擋住 → 藍色盾數字 + 藍閃;穿過 → 紅數字 + 紅閃 + 向後仰 9px;
  弱化/束縛/偷骰/消骰各自飛一個 glyph 上英雄欄。全體攻擊一次過演出四個。
- **`instant_anim`**:測試同截圖工具將所有拍子壓成 0,一場仗幾幀跑完。
  另加 `end_turn_and_wait()` 俾要等盤面停低嘅呼叫者 await;個掣本身唔等。
- 敵人行動期間三個掣全部 disable——佢哋作用嘅狀態正喺度變緊。

## 4. 升級 offer 畫面(`screen_reward.gd` 重寫)
- offer 卡由「攻4 穿刺」改成 **`Glossary.effect_sentence()` 完整句** + 目標行。
  玩家要放棄一個骰面先換到呢個面,唔應該要佢先解碼縮寫。
- 底部角色列:每個角色一粒**可撥嘅 72px `Die3D`**(free spin)+ `MiniPortrait`
  + 名。**點粒骰切 A/B**,**長按角色任何位置開 12 面檢視浮層**。
- 為咗一粒骰同時做到「撥」「點」「長按」,`Die3D._free_spin_input()` 加咗行程
  累計:放手時行程 < `DRAG_THRESHOLD` 就當 tap 發 `pressed`,按住 0.5s 發
  `long_pressed`(並標記唔係 tap)。
- 12 面浮層係**現有** `FaceSwap`,新增 `view_only` 參數(唔可選、冇確認掣、
  提示改成「長按可看詳情」),而唔係另起爐灶。

## 5. 遺物體驗(`DetailCard.show_relic` / 戰鬥左上角 / 非戰鬥入口)
- **獲得一刻一定彈卡,而且要撳確認先收**。`_present()` 加咗 `opts`:
  `dismissible = false` 令 scrim 唔再關卡,`confirm` 加確認掣,`glow` 俾高級遺物
  金邊 + 光暈(shadow 用金色、offset 0)。點解要 blocking:遺物係成個 run 生效
  嘅規則,舊版只喺結算卡一行灰字講過,玩家打完 run 都唔知自己帶緊乜。
- 覆蓋所有途徑:精英/護送小妖(獎勵畫面逐張排隊)、boss(高級二選一)、寶箱、
  商店、事件 V04 祭壇。
- **20 個專屬 glyph**(`Glyphs._r_*`),同 35 個 keyword glyph 同一套語言:32×32
  設計框、扁平填色 + 粗描邊。刻意畫成**物件**(罐、錢、鼓)而唔係符號——遺物係
  你袋入面一件嘢,而且要喺戰鬥頂部 40px 一行入面靠剪影認得出。
- 戰鬥畫面:頂欄下面一條遺物 icon 帶,高級排先,超出就橫向捲;長按任何一粒開
  全部遺物清單。非戰鬥畫面:`RunWidgets.topbar()` 個「寶N」chip 變成掣。

## 6. 長按敵人 = 完整行動預告
- **數值喺引擎度算,唔喺 UI 度估**:新 `BattleCore.forecast_enemy(j)` 同
  `forecast_target(f)` 就擺喺 `_enemy_attack_heroes()` 隔籬,一齊計弱化、蓄力、
  嚎叫、狼群加成、B2 連段(逐粒骰遞增)、B3 劍舞、B5 卸甲。UI 只負責砌句子。
- 目標寫明:全體 → 「對全隊」;有人嘲諷 → 「對嘲諷者 Ember」;冇人嘲諷 →
  「對隨機一位英雄」(誠實講明佢真係隨機,唔係扮知道)。
- 已執行 / 已取消嘅骰照樣列出,灰底 + 標註,咁玩家見到嘅係「呢回合發生咗乜 +
  仲有乜嚟緊」。
- Boss 倒數:`boss_forecast()` 計 B1 怒濤連擊(3 回合)同 B6 滅世咒(2 回合),
  寫明傷害、次數同「還有 N 回合」/「本回合結束時發動」。
- **長按同點擊唔會撞**:`_attach_longpress` 一觸發就 set `_lp_fired`,所有 tap
  handler 開頭 `_consume_longpress()`。Godot 係先 emit `gui_input` signal 先至
  行 Button 自己嘅處理,所以個 flag 一定趕得切。

## 7. Boss 卡壓住施放區(附圖 bug)
- **根因**:敵人卡係按內容自己撐高嘅,而施放區以前係一舊 360px 孤島泊喺競技場
  中間。最高嗰張 boss 卡本身就高過佢被錨定嘅敵人帶,於是兩者佔同一堆像素。
- **修法**:① 施放區改成**跨全寬嘅獨立橫帶**,夾喺敵人帶同英雄區之間;
  ② 敵人帶係一個**固定預算**,`ENEMY_CHROME_H`(274px:首領標籤、名、HP、狀態、
  意圖 chip、傷害預覽、內距、間距同美術框自己嘅 10px 頂空)減出嚟就係美術可以
  用嘅高度(`_enemy_art_budget()`)。超尺寸嘅 boss **縮**,唔會再推開施放區。
- 帶位重新分配俾返敵人區:敵人 96–570、施放 584–646、英雄 654–1014。
- **`tests/layout_test.gd`**:六隻 boss + B6 帶兩隻召喚 + 四小怪同場,每個 case
  都注入五種狀態同一張傷害預覽 chip(最高嘅情況),斷言施放區 rect 同每張敵人卡
  rect **交集為空**、卡唔會超出敵人帶上下、唔會出 720px 畫布。呢個常數就係會
  過期嘅嘢,所以佢有測試睇實。
- 截圖迴歸:`bash tools/boss_shots.sh` → `art_iterations/boss_layout/`(要真窗口)。

## 8/9. 獎勵曲線 + 遺物全面重做
- 骰面三選一:普通 C70/R25/E5 不變;**精英 C25/R60/E15**(新
  `balance.offer_rarity_elite`);boss 維持 R60/E40。`gen_offers()` 由
  `is_boss: bool` 改收 `kind: String`,三種節點三張表。
- 結算次序統一成 **金幣 → 遺物 → 骰面三選一**,`Game.on_battle_finished()` 同
  獎勵畫面都照呢個次序行。
- 遺物由 18 個換成 **14 普通 + 6 高級**。刪走 R06 荊棘圖騰(俾 A02 取代)、
  R11 軍旗、R15 血玉(併入 A03)、R18 王冠。
- **所有遺物由「效果」查,唔再由 id 查**:`GameData.relic_value(ids, effect)` /
  `has_relic_effect()`。引擎入面以前有二十幾處 hard-code `"R05"`,呢次重寫要人手
  逐個搵返出嚟——同樣嘅事唔想做第二次。
- **荊棘改成回合結束 -1**。原本荊棘永不消退,即係 A02 個「不會消退」係一句廢話,
  而 Bramble 每回合 +1 會無限疊。呢個係規則層改動(任務 9 准改),而且正正令
  A02 由「多 3 層」變成「解鎖荊棘流」——高級遺物要定義一條 build 軸,呢個就係。
- **A01 雙月徽記**:`h.used_die` 保留(存檔/相容),新增 `h.used_dice: Array`;
  `can_use()` 用「呢粒骰用咗未」+ `twin_available(i)` 判斷。名額係**用出嚟**先
  claim(邊個真係使咗第二粒骰),唔使事先宣告。
- **A04 賭徒之骨**:`can_reroll()` / `rerolls_unlimited()` 取代直接睇 `s.rerolls`;
  每次重擲全隊各 -1 HP(無視格擋),UI 個掣顯示「重擲 ×∞」。
- **A05 森之心**:新 `spell_cost(fd)`,`can_use()` 同 `use_face()` 都經佢。
- **A06 獸王戰鼓**:`s.drum` 喺 `attack_value()` 度加,喺攻擊**結算之後**先累加,
  所以係「下一次攻擊 +1」而唔係「今次 +1」。
- **舊存檔相容**:`Game._migrate_relics()` 喺 `_normalize_run()` 度行,認唔到嘅
  遺物 id 逐個換成同稀有度(普通)嘅隨機未持有遺物,數量唔變。用 run seed 派生
  嘅 rng,所以同一個存檔載入兩次得出同樣結果。

## 11. 雙語「擲出」行去重
- `Data.bi("擲出:" + Data.face_name(fd), "Rolled: " + Data.face_name(fd))` ——
  `face_name()` 本身已經係雙語,再入 `bi()` 兩邊就變咗印兩次。改成
  **標籤雙語、名字一次**:`"%s:%s" % [Data.t("ui_rolled"), Data.face_name(fd)]`。
- 全 repo 審過所有 `Data.bi(` 呼叫位,同款只有一個:`face_swap.gd` 嘅
  「XXX → 數值 +1」提示,一併修好。其餘全部係「zh 版入 zh、en 版入 en」,正常。

## 12. 雙語長句改成上下兩行(`Data.bi2()`)
呢個係做任務 6 嗰陣先發現嘅:`Data.bi()` 將兩種語言擺埋一行中間夾個空格,
對**名字**啱(「側身 Sidestep」),對**句子**就變成一條讀唔到嘅長蟲:

    機制:怒濤連擊:第 3、6、9…回合開始時預告… Gimmick: Raging Combo: announced…

新 `Data.bi2()` 一種語言一行(`Glossary._bi2` 本身一直係咁做,只係冇公開)。
凡係長過標籤嘅都改晒:敵人預告卡全部行、遺物同藥水描述、高級遺物二選一卡。
名字、chip、按鈕文字繼續用 `bi()`。

## 13. 出貨前截圖自檢(兩個新工具)
- `bash tools/boss_shots.sh` → `art_iterations/boss_layout/`:六隻 boss、
  B6+兩隻召喚、四小怪同場、B4+兩隻雜兵,每張都注入滿狀態同傷害預覽 chip。
- `bash tools/round4_shots.sh` → `art_iterations/round4/`:靈息條(顯示/隱藏
  兩種)、遺物帶、遺物獲得卡、遺物清單、小怪同 boss 嘅行動預告卡、獎勵畫面、
  高級遺物二選一。

呢輪截圖捉到四個編譯唔出嚟嘅問題,全部已修:
1. 遺物帶排成一直行——`HFlowContainer` 喺橫向捲動嘅 `ScrollContainer` 入面
   只會攞到**最小**寬度(一粒 pip),所以自己 wrap 落去。改成兩個明確嘅
   `HBoxContainer`。
2. 高級遺物二選一嘅標題同描述衝出 720px 畫布——`UIKit.title()` 唔會 wrap,
   而描述用咗絕對錨定所以文字一長就切走。改成有邊距嘅欄 + 會自己量高度嘅卡。
3. 獎勵畫面角色列嘅提示字壓住名牌。提示移到列**上面**。
4. offer 卡高度 168 唔夠放「四行效果句 + 目標行」,最後一行被 `clip_contents`
   切走。改 200。

---

# 第三輪:角色大改版判斷記錄 — 2026-08-06

## 角色身份對應(規格冇寫檔名對應)
規格明令「唔准靠檔名估」,所以六張圖逐張打開睇過內容。實際對應同規格列出嘅次序**唔一樣**:

| 檔案 | 物種 | 角色 |
|---|---|---|
| `character_1.jfif` | 獾(2×2 四姿勢 sheet,裁左上) | 獾斧衛士 |
| `character_2.jfif` | 狐狸(藍圍巾、雙彎匕) | 狐影雙刃 |
| `character_3.jfif` | 貓頭鷹(琥珀晶石法杖) | 梟賢者 |
| `character_4.jfif` | 野豬(巨石鎚) | 蠻豬破軍 |
| `character_5.jfif` | 野兔(長弓+箭袋) | 野兔神射手 |
| `character_6.jfif` | 刺蝟(圓木盾+釘錘+鎖甲) | 刺蝟盾衛 |

## 新角色 ID
用可讀 ID(`BADGER`/`HARE`/`HEDGE`/`OWL`/`FOX`/`BOAR`)而唔係沿用 `H1`…`H6`。
規格要求「舊角色 ID → 新角色 ID」對應表,即係預期 ID 會變。順帶把全部
`["H1","H2",…]` 硬寫清單換成 `GameData.hero_ids()`(由 `heroes.json` 嘅 `order` 排序)
—— 上次換角色時,搵晒呢啲清單本身就係大半日工夫。

## 四個新 keyword 之中,兩個原本已經存在
- **自損 Recoil** = 現有 `pain`(中文本來就叫「自損」,只改英文名 Pain → Recoil)
- **穿透 Pierce** = 現有 `pierce`(英文本來就係 Pierce,只改中文名 穿刺 → 穿透)

兩者機制完全一樣,所以**重用**而唔係另開一套 —— 開兩套會令同一條規則有兩個圖示、
兩段解釋。因此新畫嘅 glyph 係四個**真正新嘅**符號:蓄力 `chargeup`、呼應 `resonate`、
多重攻擊 `multi`、**鎖定 `lock`**(鎖定由「重擲時嘅方便」升格成核心資源,而蓄力/
呼應/換位/雙舞全部讀佢,所以佢需要自己嘅符號)。

## 撞名處理
- 敵人 keyword `charge`(蓄力 Charge)同新玩家 keyword 撞晒中英名 → 敵人嗰個改名
  **凝力 Gather Power**(只出現喺敵人意圖,影響面最細)。
- 現有 `echo`(迴響 Echo)同新 keyword 呼應 Echo 撞英文名 → 舊嗰個英文改
  **Reverb**,`sp_echo_crystal` 一併改為 Reverb Crystal。

## 呼應嘅判定
規格寫「如果鎖定嗰粒骰當前面係攻擊面」。解讀為:**同一位英雄嘅另一顆骰必須處於
鎖定狀態,而且顯示指定類別嘅面**。判定喺**使用時**做,唔係擲出時 —— 所以中途先鎖
拍檔骰一樣有效。呢個解讀令鎖定變成要付出嘅資源(放棄一個重擲目標),同「雙舞」
「換位」直接操作鎖定機制嘅設計一致。

## 鎖定跨回合保留(規則改動)
蓄力寫明「每喺鎖定位渡過一個完整回合」,所以鎖定必須跨回合。連帶:
- 回合開始時,**只有未鎖嘅骰**會重擲;鎖住嘅骰保留面同層數。
- 用掉、解鎖、重擲都會清空層數(「使用後歸零」)。
- 用過嘅骰下回合照樣重擲,**除非**「雙舞」明文保留(`keep_lock`)。
- 被偷骰(B4)會令該回合冇骰,層數亦會清空 —— 判定為 B4 應有嘅破壞力,冇特別保護。

## 多重攻擊 X×N 嘅細節(規格冇寫)
- 每一下**獨立扣格擋**(所以對高格擋敵人差)。
- 每一下**獨立食荊棘同反擊**。
- 附帶狀態(中毒/灼燒/弱化/易傷)**只喺第一下施加**,唔會疊 N 次。
- 固定加成(磨刀石/戰吼/暴走/藥水)**每一下都食**,即實際 ×N。刻意保留。

## 被動:規格冇寫,由實作決定
規格寫咗六個定位同圖鑑文,冇寫被動。三個沿用被映射舊角色嘅被動,三個新寫。
理據同數值見 BALANCE.md 第三輪。

## XP 池排序
規格列出六個 XP 面但冇指定解鎖次序 → 由弱到強排(便宜嘅先解)。
曲線由 5 級延伸到 7 級,沿用同一條等差。

## 存檔遷移
- **meta.json**:XP 原數轉移、解鎖狀態按映射、圖鑑 flag 按面對應表轉移。可重複執行。
- **run.json**:進行中嘅 run **會遷移而唔係丟棄** —— 角色 ID 換,12 格逐格重坐
  (角色專屬面換成新角色同格嘅面,玩家自己買返嚟嘅共用池面原地保留,因為冇一個
  共用池 ID 被移除)。HP 按**比例**帶過,唔係直接搬數字。
- 舊 XP 池面按**池內序號**對應(規格明文)。有幾個舊 ID 同時出現喺起始骰同 XP 池
  (例:貫穿箭)→ 池嘅對應優先,因為池嗰個先係玩家「賺返嚟」嗰個。

## 美術
- 去背用 `tools/art_cutout.py`:邊界 flood fill(靠六張圖共通嘅深色描邊擋住,
  唔會食到內部淺色毛髮),加一個「封閉口袋」偵測(野兔弓同弓弦圍住嗰塊底色
  由邊界入唔到)。門檻係**量出嚟嘅**,唔係估:真背景口袋嘅色差 <3、毛髮 >16。
- 六張色調對齊到全體平均值,強度 0.75 —— 拉到 1.0 會令狐狸嘅橙同刺蝟嘅鋼藍
  變成同一坨啡色。
- 小怪同 boss **維持程序繪製**,唔喺本輪範圍。

---

# 第四輪:敵人腐化大改版判斷記錄 — 2026-08-07

本輪只動視覺(`PawnArt` / `UITheme` / 兩個擺放呼叫點 / 測試 / 工具)。
`data/` 一個字都冇改,`battle_core.gd` 一行都冇改 —— 數值同規則完全冇掂。

## 敵人身體色比參考圖淺一大截(最重要嘅一個取捨)
參考圖六隻 boss 同十隻小怪都畫到近乎純黑紫,但**嗰啲圖係畫喺米白背景上**。
遊戲入面敵人企喺 `UITheme.surface(chapter)` 嘅卡入面,第 3 章嗰塊係 `#1e1429`。
量過:近黑嘅身體對第 1 章卡面(`#1b3019`)只有 **1.4:1**,即係卡入面一個窿。
連 `#2b2b2b` 描邊都同第 1 章卡面差唔多一樣光,**描邊喺嗰度等於冇**。

所以身體色統一提到暗中間調,設計目標 2.55:1,測試門檻 2.4:1
(`_t_enemy_legibility`,量最深嘅 T3,對三章卡面各量一次;實測最差 E04 對第 1 章 2.54:1)。
「腐化」感改由**去飽和 + 暗紫底 + 桃紅裂紋 + 黑霧 + 描邊**去撐,唔靠純黑。
E05 暗影蛾同 E08 怨靈本身最深,另外加**輪廓光**(`ROT_RIM`);六隻 boss 亦一律有。

## Tier 用「腐化深度」畫出嚟,唔淨係數字
`rot_of()` 把 tier 攤成 0→1,同一時間推五個掣:
體型 `TIER_BULK` 0.90 / 1.00 / 1.12、色調(T1 提亮 0.14,T3 向 `ROT_UNDER` 拉 0.28)、
裂紋條數(1/3 → 2/3 → 全部,T3 再加一層柔光)、眼睛亮度、黑霧(**T1 完全冇霧**)。
五樣一齊郁,所以三個 tier 唔使並排都認得出;並排(圖鑑)更加係一眼分。

`EXTENT` 統一喺 **tier 3** 量(最大嘅一個),低 tier 就自然喺同一張卡入面畫細啲 ——
即係「第 1 章嘅怪細隻、第 3 章嘅怪大隻」呢件事喺實戰入面真係睇得出。
地面陰影另外用 `half_width(kind, tier)`,唔會出現 T1 小怪拖住 T3 闊度嘅影。

## Magenta 由本輪起係敵方專屬色
`UITheme.is_magenta()` 定義色域:hue **285°–342°**、s ≥ 0.35、v ≥ 0.30。
落界喺 285 而唔係 270,係**特登留咗一段緩衝**:遊戲原有嘅紫色家族
(`PURPLE` 資源色、第 3 章 bg/surface/accent)全部坐喺 **260°–270°**,
留 15° 空隙,一個色調微調唔會靜靜雞滑入敵方色域而冇人知。

`_t_no_player_magenta()` 唔係人手列清單,而係行 `ui_theme.gd` 嘅
`get_script_constant_map()` —— 下個月加嘅新色,加嗰日就自動受管。
`ROT_*` 係唯一例外,而且測試**反過來要求佢哋一定要喺色域入面**
(規則要有意義,保留嗰邊就要真係用)。
六張手繪英雄圖版另外逐粒像素掃(step 3):實測 0 粒落色域,乾淨。
掃之前先查過全 project 90 隻 hex,只有舊 E03 菇帽 `#b06ac2`(h=288)踩界,而佢本身就係要重畫嘅敵人。

## `_limb` 加曲線平滑 —— 第一版「碎片」嘅真兇
藤蔓(E06)第一版變咗一堆黑色碎片,唔係配色問題:`_limb` 用法線向兩邊撐開,
遇到急轉角兩條邊會**交叉**,polygon 自我穿插就爆成碎片。
加咗 Catmull-Rom 平滑(`_smooth`)之後,藤蔓、鼠尾、蛇頸、boss 手臂、法師袍袖
全部一次過修好 —— 呢個係本輪唯一一個「一改改晒」嘅修正。

## 石甲蟲 E04 面部淡化
參考圖佢塊前板刻住貓頭鷹紋,但玩家隊入面有梟賢者,兩張卡隔兩吋就撞樣。
所以重畫成:頭細、縮喺兩塊肩甲之間、只留一條窄 visor 同兩點細眼,
辨識度全部交畀**龜甲同肩甲**。冇面盤、冇眉、冇喙。

## 荊棘藤 E06 冇面 → 補一對眼
參考圖十隻之中只有佢完全冇五官。但「發光桃紅眼」係本輪敵方統一語言,
冇眼就出局。判斷:保留參考嘅「一舊纏藤」剪影,喺纏得最實嘅結心塞一對細眼。

## B3 兩階段用 art key 分,唔郁 `e.key`
`e.key` 全程都係 `B3`(圖鑑、遇敵 flag、數值表全部歸檔喺呢個 key 底下)。
新增 `screen_battle._art_key()`,只喺 `phase >= 2` 時轉用 `B3P2` 呢個**淨美術** key。
`battle_core` 一行冇改。階段 2 特登畫到**更窄更高**,落地嗰下卡入面個剪影會明顯變形。

## 圖鑑補上美術
規格話「圖鑑用同一套 PawnArt 自動更新」,但小怪/boss 面板本來**淨係文字、一張圖都冇**,
「自動更新」等於冇嘢更新。所以補咗 `_tier_strip()`(三個 tier 並排,未打過嗰個維持剪影,
同解鎖邏輯一致)同 `_boss_strip()`(B3 出兩格:騎鵝 / 落地)。純顯示,冇改解鎖規則。

## 黑霧改成灰紫
`ROT_MIST` 原本 `#241a2e`(近黑)。第 3 章卡面本身就係近黑,黑霧完全睇唔到 ——
而黑霧係分 tier 嘅五個掣之一,睇唔到就少咗一個掣。改成 `#4a4055` 灰紫,
三章都見到,亦冇踩入 magenta 色域(s=0.25,唔夠 0.35)。

## 驗證
- `tools/pawn_extents.gd` 改成**逐 tier 量**,印晒三個 tier,存 tier 3 嗰組。
  新增 `B3P2`。四次改圖之後重量咗四次,最後一次貼返入 `EXTENT`。
- 新工具 `tools/pawn_gallery.gd`:10 小怪 × 3 tier + 6 boss(B3 兩階段)= **37 張**,
  逐張都係「章節天空 + 章節卡面 + 企喺卡上」嘅真實構圖,另加四張 contact sheet。
  期間迭代咗六輪(`art_iterations/rot_1` … `rot_final`),逐張覆核。
- `gallery_export.gd` 加咗 `15_battle_ch1/2/3`:同一組小怪(E01/E05/E08)喺三章實戰,
  一次過驗 tier 差異同對比度。540 抽驗:三隻剪影仍然各自認得出。
- 全部 12 個套件綠(`bash tools/test_all.sh`),`_t_contrast()` 照樣過。

---

# 敵人 sprite 改版(2026-08-07 → 08)

規格鐵律:**唔准手畫任何生物造型。** 10 隻小怪 + 7 張 boss 圖(連 B3 第二階段)
全部由 `Art reference/` 嘅 AI 生成圖裁出嚟做 sprite,`pawn_art.gd` 入面約 700 行
手畫 routine 全刪。以下係規格冇寫、由實作決定嘅事。

## 「墨線輪廓 + 填內部」係呢批 AI 圖嘅通用刀法
唔用「非奶白 = 主體」嗰招。呢批圖有一個穩定嘅生成特性:**主體必定被一條封閉深墨線
包住,雜物必定冇**。所以取墨線遮罩(value < `INK_V`)嘅最大連通區域、連埋所有 ≥ 5%
嘅部件,再 `binary_fill_holes`。一招同時殺三樣嘢:黐喺身上嘅灰煙(冇墨線)、藤內圈
嘅奶白窟窿(被填)、右下角 ✦ 水印同飛濺點(唔夠 5%)。
**以後所有同源 AI 圖都用呢把刀,唔好返轉頭做 cream key。**

## 填出嚟嘅窿要再用顏色判一次(`_background_pockets`)
`fill_holes` 唔分「真係身體內部」定「擺姿勢露出嘅背景」。B4 膊頭/角之間、B3P2 兩邊
腋下、E06 藤圈,都係俾墨線兩邊夾住嘅真背景。所以只對 fill 加出嚟嗰批像素(**永遠唔
掂墨線本身**)做兩段背景測試:去飽和又光(`sat<0.20 & val>0.63`,捉住帶陰影嘅背景),
或者平又貼近圖邊中位色(`std<14 & 差<6`,捉住乾淨奶白)。判斷落喺侵蝕 1px 之後嘅核心,
因為墨線化開嗰個環兩樣都唔係。

## 掛喺墨線外面嘅飽和發光要收返入嚟(`_attached_glow`,2026-08-08)
E07 骨狼嘅眼火同 B6 嘅符文弧係畫喺**封閉墨線外面**嘅,單靠輪廓法會將佢哋切走,
剩返自己嗰條細墨線圍住一個窿。實測 E07:全張圖 14,345 個飽和亮像素,得 4,870 個
喺骷髏輪廓入面,而且 `INK_V` 由 0.30 掃到 0.48 只郁到 102 個 —— 即係「真係喺外面」,
唔係 E05 嗰種「墨線斷咗」(嗰種 `INK_V` 醫得到)。
判準係**飽和度**:呢個 pipeline 要掉嘅雜物 —— 煙(sat < 0.15)、✦ 水印、飛濺 ——
全部係去飽和嘅,奶白 sat ≈ 0.08。所以「飽和 + 亮 + 貼住墨線」收,其餘照掉。
全套 17 張只郁兩隻:E07 +8,930 px(兩束火完整)、B6 +3,202 px(四段符文弧),
其餘全部 0。

## 手畫造型 routine 全刪,唔留 fallback
留住唔係「保險」,係一個瞓喺度嘅後門:貼圖 load 失敗就自動畫返手畫造型,
直接違反鐵律。`_draw()` 而家搵唔到 plate 就乜都唔畫。要考古有 git history。
`_mist`/`_wisp` 保留 —— 佢哋畫嘅係煙同光,唔係生物造型。`_rim` 刪咗,rim 而家係 shader 做。
黑霧位置改由 sprite alpha 闊度自動撒,唔再逐隻手寫座標(手寫座標綁死喺舊造型上面,換圖就錯)。

## 認色搬去 pipeline,唔喺 shader 做(呢個推翻咗原設計)
原本設計係 shader 自己用 `value > 0.72` 分「眼」同「腐化紋」。量完數推翻:
眼嘅 p25 = 0.49、腐化紋嘅 p50 = 0.49,兩者完全重疊,喺 eye-p25 落刀有 **48.9%**
腐化紋會被誤認成眼;洋紅明度直方圖單調下降冇谷,單獨睇 9 張 1024px solo plate 一樣冇。
**呢批圖本來就將眼同紋畫喺同一個明度區間。**
分別喺**形狀**:眼係緊實團塊,腐化紋係幼線網絡,腐蝕兩格再還原就一刀切得開。
fragment shader 冇連通區域分析,做唔到 —— 所以分類搬入 `tools/enemy_cutout.py`,
每隻烘焙一張 `<KEY>_rot.png`(R 眼、G 紋、B 岩漿),shader 淨係取樣同乘 gain。
用家揀「認色自動發光」時要嘅性質全部保住:零手工資料、換圖唔使重新量、
雙頭蛇四隻眼同蛾嘅細眼自動處理。代價係每隻多一張遮罩貼圖。

量出嚟嘅界線:`MASK_HUE` 0.80–0.91、`MASK_SAT_MIN` 0.45、`MASK_VAL_MIN` 0.35。
`MASK_VAL_MIN` 係後加嘅方法學修補:墨線係暗紫紅,色相落喺洋紅帶,又因為暗而報高
相對飽和度 —— 唔設明度下限佢會佔咗 56% 嘅「洋紅」像素。
橙帶(岩漿)單獨一條路,因為蟾蜍嘅裂紋係「熱,唔係腐」,呢個身份要保住;
單靠 hue + sat 會誤中 B2 拳套嘅啡皮革(命中數量多過蟾蜍真岩漿),所以要加 `EMBER_VAL_MIN` 0.62 ——
岩漿係著住嘅,皮革唔係。

## rim 係內側唔係外側
sprite trim 到 alpha bounds,`draw_texture_rect` 冇 padding,向外嘅光會俾 rect 切走。
所以 rim 畫喺剪影**內側**:`edge = src.a * (1 - amin)`。順帶好處係 rim 正正落喺圖本身
嗰條粗墨線上面 —— 墨線係最暗嘅部分,亦即係喺近黑卡面上最易溶掉嗰部分。

## 可讀性有兩把尺,而且對過數
- **代理**(headless、全量、留喺 `ui_smoke`):讀 `enemies.json` 嘅 `edge_rgb`,
  按 shader 同一條公式同 `rot_rim_for(ch)` 混合,再對 `surface(ch)` 計對比。
- **實測**(要視窗):`tools/enemy_legibility.gd` 真 render 落三章卡面讀返像素。
- **收貨條件係兩者對數**,差距門檻 0.15:1,超過**以真 render 為準**,返轉頭校代理。

2026-08-08 收尾嗰次實測結果(七張補圖 + `INK_V` 0.32 之後):
`BOUND1 CALIBRATED`,worst delta **0.046** at E07 ch3(上限 0.15);
代理對真 render 嘅樂觀偏差 **+0.0144** at B3P2 ch2,`CARD_EDGE_MARGIN` 0.06 **COVERED**;
`BOUND2` 霧覆蓋最差 **14.8%**(上限 25%);`BOUND3` 霧背邊緣最差 **1.735:1**(下限 1.55)。
37 行卡背對比全部 ≥ 2.40,最差 2.508:1(E03 ch2)。

一條界喺呢一輪由三條變成:卡背 2.4:1、霧覆蓋 ≤ 25%、霧背 1.55:1。
原因係「2.4:1」單獨一個數會被一個「用霧遮住成條輪廓」嘅實作滿足 —— 遮住就冇邊緣可量。
霧背下限 1.55 係由真 render 定價嘅(Task 5 嗰個 1.9 係由 `edge` 釘死喺 1.0 嘅模型推出嚟,
唔算數)。

## `ROT_RIM_TARGET` 由 2.4 加到 2.46(2026-08-08)
呢個常數解嘅係**釘死模型**(純黑邊緣 + 全覆蓋),但套件真正 assert 嗰條界係
`seen_edge` —— 縮細之後、按自己 alpha 合成嘅band,再加 `CARD_EDGE_MARGIN` 0.06。
兩個唔係同一個數。合集格年代冇人夠黑所以睇唔出:合集邊緣帶約 (33,23,38),
solo plate 暗到 E03 嘅 (18,12,26),喺 2.4 之下 E03 ch2 出 2.453:1,
過到呢個常數寫嘅目標、過唔到套件揸嗰條。解返套件真正 assert 嗰條先算誠實。

## 補圖:七張 solo plate(2026-08-08)
合集每格只得約 250px,JPEG 將幼裂紋同細眼壓到又暗又灰,而 shader 讀嘅正正就係
嗰批已經冇咗嘅像素。實測:solo plate 亮洋紅佔 2.17–7.31%(9/9 認到眼),
合集格 0.22–1.05%(3/8),E01/E04/E07/E08/E10 **一隻眼都認唔到**,
tier 遞進喺佢哋身上完全著唔起 —— 即係由「外觀」升級做「功能阻斷」。
七張補圖到咗之後,呢七隻嘅眼+紋物料由 0.22–1.05% 升到 **2.40–6.33%**。

**E04 石甲蟲照用新圖。** 交代落嚟嗰陣話佢冇新圖、叫用返合集版本,但
`Art reference/monster/beetle.jfif` 其實喺度,而且係合規嘅 1024px 奶白底 solo plate:
造型對齊合集(重甲龜背、頭縮喺兩塊肩甲之間、窄 visor 兩點細眼,**冇貓頭鷹紋**,
冇撞到玩家隊嘅梟賢者),洋紅眼同裂紋夠飽和。用返合集版會令佢繼續係全套最差嗰隻
(0.28% 眼+紋,gallery 入面兩隻眼全暗),而用新圖直接跳到 6.33%。
所以用新圖,E04 亦都由補圖清單剔走。

**E09 熔岩蟾係全套最後一張合集格,而且唔係欠圖。** 佢靠橙色岩漿嗰條路
(1,217 個 ember 像素 vs 323 個眼+紋),洋紅門檻對佢係量錯嘢。
所以佢留喺 `PENDING_ART`,但註明係「量度口徑」而唔係「等圖」——
有新圖唔會令佢過關,改量法先會。

## E05 咬爛翼:`INK_V` 0.30 → 0.32(2026-08-08)
`binary_fill_holes` 對斷咗嘅輪廓係全有全冇。蛾喺 (x885, y383) 嗰度個翼溶咗做噴筆
灰塵,成條 20+ px 闊嘅帶完全冇墨線,奶白就行咗入嚟,再擴散 —— 因為蛾係全套唯一
一張身上顏料暗到似墨線嘅圖(翼峰喺 0.28–0.31,到 0.32 跌到剩返 11/千)。
代價 12,440 px 翼內部,即係剪影嘅 6.4%,分三啖咬。
0.32 之後窿位 12,440 → 3,178,17 張輸出嘅尺寸同 aspect 一個都冇郁。
呢個改動 2026-08-07 押後過,原因係佢會郁 E05 自己同 B2 兩隻嘅 `edge_rgb`,
要重跑成個代理/實測對數 —— 而補圖本身已經逼住要重跑,所以一次過落。
**重跑 `enemy_legibility` 之前一定要迫 Godot 重新 import**,否則佢 render 緊舊貼圖,
會報返一模一樣嘅舊數。

## E05/E06 用 solo plate 唔用合集中間格
合集中間格上蛾下藤,實測兩件根本冇黐埋(切得開),但只得 145/203px,
而且合集嘅藤內圈有奶白窟窿。solo 係 1024px,兩個問題一次過冇。

---

# Web 試玩版同上線(2026-08-08)

## 遊戲要自己帶字體(呢個係 web 版最大嘅發現)
本來成個專案冇 font asset:Godot 內置嘅 Noto Sans 出 Latin,中文由作業系統
fallback(Windows 係 Microsoft JhengHei)。瀏覽器**冇系統字體可以 fallback**,
所以第一個 web export 出嚟成版中文係豆腐格,連標題都係。

判斷:砌一隻**只含遊戲用到嘅字**嘅字體,入 `gui/theme/custom_font`。
`tools/font_build.py` 掃全 repo 嘅 `.gd/.json/.tscn` 收集字元(959 個 CJK,
共 1095 個),由三隻 SIL OFL 字體裁出再合併:

| 來源 | 供乜 |
|---|---|
| Noto Sans TC | 全部中文 + Latin |
| Noto Sans Symbols 2 | ✦ ✕ ✗ 💰 ⭮ |
| Noto Emoji | 🧪 |

5.7 MB 變 392 KB。Godot 本身支援 fallback font 鏈,可以唔使合併,但 fallback
清單要喺 `.import` 檔手改、錯咗會靜靜哋唔生效;砌成一隻檔可以**驗 cmap**,
所以 `font_build.py` 覆核唔齊就唔寫檔。

**`↻` U+21BB 換成 `⭮` U+2B6E。** U+21BB 喺任何一隻 Noto 都冇 —— 佢一直行到
淨係因為 Windows 有 Segoe UI Symbol。同一個箭嘴,換去帶得走嗰隻。

副作用:`ENEMY_CHROME_H` 由 274 加到 281。換字體換咗 line height,B6 張卡撐到
577 過咗 570 條界,`layout_test` 即刻紅。呢個係測試做緊嘢 —— 重新量,唔係放鬆條界。

**一個踩過嘅坑:`project.godot` 嘅註解係 `;` 唔係 `#`。** 用 `#` 寫註解會令
Godot 掉埋成個 section,`gui/theme/custom_font` 靜靜哋讀唔到,行為同冇設過一樣。

## 非多線程 web export,唔行 coi-serviceworker
Godot 多線程 web export 要 `SharedArrayBuffer`,而佢要 document 係
cross-origin isolated,即係要 COOP/COEP response headers —— GitHub Pages
**根本唔俾設 header**。業界 workaround 係 coi-serviceworker(用 service worker
假扮 isolation),但佢首次到訪要多行一次完整 page load、喺 service worker
註冊唔到嘅情況下會直接冧,而且係多一件要養嘅嘢。
`variant/thread_support=false` 由根本上免咗呢個要求 —— 兩條路之中喺 Pages 上
真係穩嗰條。實測:載入 1.3–1.8 秒(本機),零 console error。

## Pages 用 `docs/` 資料夾,唔用 GitHub Actions
Actions 要每次 build 都載 Godot + export template(約 1 GB),多咗一個會壞、
而且壞咗唔容易 debug 嘅環節。`docs/` 就係「build 完 commit 落去,push 即上」,
本機驗到乜,線上就係乜。代價係 binary 入 git history(約 46 MB/次)。
`bash tools/web_build.sh` 係唯一寫 `docs/` 嘅嘢,`docs/.gdignore` 阻止 Godot
將 exporter 自己嗰啲 icon PNG 當資源 import 返入專案(舊嗰個 root-level export
就係咁留低咗 `index.png.import` 喺 repo 度)。

順手清咗 repo root 嗰個舊 web export(`index.html`/`index.js`/`index.wasm` …)。
佢係壞嘅:`.gitignore` 有 `*.pck`,所以 `index.pck` **由頭到尾冇入過 git** ——
Pages 服務緊一個永遠載入唔到嘅頁。

## export 排除參考圖同截圖
`export_filter="all_resources"` 會將 `Art reference/`(22 MB)、`qa/`(8 MB)、
`final/` + `final_round1/`(27 MB)全部打包 —— 全部係工具輸入同截圖產物,
遊戲 runtime 一個都唔會 load。加 `exclude_filter` 之後 pack 由 **32 MB 跌到 6.7 MB**。

## 3D 骰喺 web 唔降級
規格話「明顯掉幀就將 3D 骰喺 web 降級做『擲骰期間先 render』」。實測唔使:
真 GPU(RTX 3070 Laptop,ANGLE/D3D11,540×960)之下 ——

| 場景 | fps | 最差幀 | 超過 33 ms 嘅幀 |
|---|---|---|---|
| 入戰鬥、8 粒骰一齊擲落枱 | 60.0 | 17.3 ms | **0 / 240** |
| 戰鬥待機 | 60.0 | 17.5 ms | **0 / 180** |
| 結束回合 → 敵人行動 → 勝利 → 轉場 | 56.2 | 133 ms | 4 / 337 |

即係話 8 骰齊擲鎖死 60fps,一格都冇跌;唯一嘅頓喺**場景轉換**(6 秒入面 4 格),
係載入成本唔係 render 成本,降級 3D 骰醫唔到佢。
**未測到嘅:低階手機 GPU。** 呢度冇裝置可以量,所以呢個結論嘅範圍就係桌面級 GPU。

## 存檔喺 web:`user://` → IndexedDB,已驗證
Godot 喺 web 將 `user://` 掛喺 IndexedDB(`/userfs`)。實測開一場 run、
refresh 成頁,標題畫面照樣出「繼續 Continue」—— meta 同 run 存檔都仲喺度。
代價寫咗入 README:換瀏覽器 / 無痕 / 清網站資料 = 存檔冇咗。

## Repo 名沿用 `game-dicebuilder`
交代落嚟話「如未有 remote 就開個 public repo,名建議 dice-grove」。
Remote 已經存在(`jackncw/game-dicebuilder`,public,Pages 已開),
所以用返佢:改名會斷咗現有 URL,亦要多一重權限。

## Pages 一定要落 `docs/.nojekyll`
Pages legacy builder 預設會行 Jekyll。第一次上線之後實測:根目錄出咗一個
**Jekyll 用 repo README 生成嘅主題頁**,蓋過咗遊戲自己嗰個 `index.html`,
而 `index.wasm` / `index.pck` / `index.js` **全部 404** —— Jekyll 唔會 copy
佢唔認識嘅檔。加 `.nojekyll` 之後即刻正常(`/` 6,070 bytes = 我哋隻 shell、
wasm 200 `application/wasm`)。`tools/web_build.sh` 每次 build 都會補返呢個檔。

順帶量到:Pages 出 wasm 有 gzip,39.5 MB 傳輸得 **10.2 MB**。所以「首次載入
46 MB」係磁碟數;實際落線約 17 MB。

---

# 第六輪:手機視窗、骰面資訊、靈息大改(2026-08-10)

## 任務1 — 手機頂欄

### 頂欄唔見咗嘅成因有兩個,唔係一個
真機回報「回合數/靈息條/重擲數成條頂欄唔見咗」。拆開係兩件事:

1. **canvas 高度用錯咗。** Godot exporter 預設 `canvasResizePolicy: 2`,即係將
   canvas 設成 `window.innerWidth × window.innerHeight`。手機瀏覽器嘅
   `innerHeight` 係**大 viewport**(地址欄縮埋之後嘅高度),而我哋隻遊戲永遠唔
   scroll,所以地址欄一直喺度 —— canvas 畫得高過實際睇到嗰條,多出嘅部分由底
   切走,而地址欄由上面蓋住。答案係 `visualViewport`(佢先係「而家塊玻璃上面
   有幾多」),配 `canvasResizePolicy: 0` 叫引擎唔好掂 canvas。
2. **瀏海同 home indicator。** shell 一直要 `viewport-fit=cover`,因為出兩條黑
   邊比背景畫到玻璃邊差。代價係 canvas 頂底兩條有硬件蓋住,深度要問
   `env(safe-area-inset-*)`。

**判斷:唔用 letterbox,改為遊戲自己 inset HUD。** canvas 照樣鋪滿整塊玻璃(森林
背景畫到瀏海底下,好睇),而**每個畫面**負責將自己嘅 HUD 縮入安全區。呢個決定
先令「高度唔夠要壓縮邊個區」變成一個真問題 —— 所以任務1嘅錨定規則有嘢做。

### 安全區預算:163 canvas 單位,而真機最深嘅係 162
`_solve_zones()` 將每條帶壓到自己嘅下限之後,剩返俾 inset 用嘅係 **163 單位**。
今日最深嘅真機幾何 —— 360×640 可視區、47px 瀏海、34px home indicator,換算
94 + 68 —— 係 **162**。**淨賺 1 單位。**

呢個數冇加水:720×1280 嘅版面喺今日啲手機上面就係咁貴。超過預算就**夾**
(先夾底部),即係 home indicator 會壓到 tray 下緣 —— 換返嚟嘅係戰場照樣守住
「敵人卡唔可以生入施放帶」呢條合約。冇一部出貨手機接近呢條線(要食掉四分一塊
玻璃先得),但唔想留一個「拖到某個 inset 就崩」嘅洞。

### 壓縮次序:敵人區 → 施放帶,頂欄同英雄區永不讓
規格寫明「優先壓縮敵人區,唔准犧牲頂欄同英雄 tray」。實作次序:
敵人帶食晒(藝術自動縮細,`_enemy_art_budget()` 本身就係做呢件事)→ 敵人帶到咗
`ENEMY_CHROME_H + ENEMY_ART_MIN` 先輪到施放帶讓(78 → 44 為止)→ 再唔夠就夾 inset。
`tests/layout_test.gd` 逐條 assert 呢個次序,唔止 assert 結果。

### 其他畫面:靠 `Safe.pin_*` 註冊表,唔逐個畫面聽事件
12 個畫面掛頂部標題、9 個掛底部 tray,全部都要跟地址欄郁。與其教每個畫面聽
`Safe.changed`,不如將 control 交俾 `Safe` 註冊一次,inset 一變就重新套用;
control free 咗就自動 drop。`UIKit.footer()` 一改,9 個畫面一齊修好。

### 順手執到嘅兩個舊 bug(唔關手機事)
- **選角畫面 tray 高度不足。** 兩行掣(84 + 64 + 12 分隔 = 160)塞入 168px tray,
  扣咗 margin 得 136 —— PanelContainer 會俾細路仔至少佢嘅最小尺寸,所以「取消」掣
  **本來就已經掛咗喺 canvas 底出面 24px**,同 inset 冇關。168 → 196。
- **`layout_test` 0 個 assertion 都報 OK。** 戰鬥畫面 compile 唔到嗰陣每個 case 都
  喺第一個 check 之前就死,結果印「0 tests, 0 failures / LAYOUT OK」。綠燈報零工係
  最壞嘅結果,加咗下限。

## 任務2 — 骰面效果

### 速記 pip 嘅預算:一粒骰 76px,540 版即係 57 物理像素
所以:最多 3 個 pip、flag 類唔印數字(圖本身就係全句)、**代價 pip 排喺裝飾 pip
前面**(冇位唔可以食咗「呢個面要收你幾多」)。pip 用 1px 描邊而唔係 `UIKit.chip`
嘅 2px —— 冇描邊喺英雄卡上面糊成一撻,2px 就食咗 76px 入面嘅 4px。

字級揀 `F_BODY_SM` 而唔係 `F_CAPTION`,即使 3 個 pip 塞唔落 —— `_fit` 會等比縮,
而**大部分面得 1-2 個 pip**。為常見情況揀大字、罕見情況先縮,好過為最壞情況將
每個面都縮到同最壞嗰個一樣細。

### 施放帶 62 → 78,錢由敵人區出
效果句係雙語,最壞情況(攻擊 + 中毒 + 穿刺 + 靈術費)係一行中文兩行英文。62px
入面塞唔落。加 16px,敵人區由 474 → 458,首領藝術由 193px 高變 177px。**呢個係
真代價**,換返嚟嘅係玩家唔使長按八粒骰先讀得到自己手上有乜。

### 一個 `clip_text` 幾乎令成個功能靜靜咁死咗
第一版喺 `cast_label` 落咗 `clip_text = true`。Godot 入面帶 `clip_text` 嘅 Label
最小高度報 1px,而 PanelContainer 只會俾細路仔佢嘅最小尺寸 —— 出嚟係一條**文字
完全正確但高度 1px** 嘅帶,截圖睇落就係空白。改為真嘅 fit:
`_cast_font_for()` 用 `get_multiline_string_size` 逐級試,揀到落嘅最大字級。

### 動作掣由 F_H2 降到 F_BODY
掣行多咗第四個控件(靈息重擲),而 Button 永遠唔會窄過自己嘅字。喺 F_H2,四個
雙語 label 加埋 749px,720px canvas 塞唔落,「結束回合 End Turn」掛咗出右邊。
落一級字級買返 90px,而 22pt 喺 540 版都仲有 16.5 物理像素。

### 星隕 English 改名
規格叫新嘅梟解鎖面做「星落 Starfall」,但佢原有嗰個「星隕」English 就係
"Starfall"。兩個唔同效果嘅面同名,對淨玩英文嘅人就係一個 bug。**改舊嗰個**:
星隕 → "Comet Fall"(字面就係墮落嘅星),同 "Starfall" 及 8 級嘅 "Meteor Shower"
都分得開。新面完全跟規格叫 星落 / Starfall。

## 任務3 — 靈息

### 靈息條唔再有「隱藏」狀態
以前party 冇任何靈息面就收起成條 bar(一條永遠郁唔到嘅 bar 讀落似壞咗)。U1 +
U2 之後呢個問題唔存在:每隊每回合都收 1 點,每隊都可以換重擲。`_party_uses_essence()`
留低並且照 call —— 佢係記住「點解而家係無條件」嘅唯一地方。

### 引靈瞄準:用「主數值食 face_bonus」呢條現成規則,唔開特例
規格寫「靈息+1,若此骰已蓄力,每層額外靈息+1」。實作唔係新機制:格擋同治療本來
就有「主數值先食骰位加成」嘅規則,將 `mana` 加入同一條規則之後,呢個面就係
`{mana: 1, charge_up: 1}`,一行 code 都唔使為佢寫。

### 靈息迴環門檻定 3
「回合結束時剩餘靈息 ≥ N」。定 3(高過 `MANA_REGEN + 1`)先至係獎勵**真係揸住**
靈息;定 2 就變成乜都唔做都每回合出糧。

### 導靈杖用「查詢」而唔係「找數時先減」
`spell_cost()` 本身回答折扣價,唔係喺俾錢嗰刻先扣。因為 `can_use()` 同 UI 都讀
同一個 function —— 顯示嘅價、灰唔灰、實際收嘅價,三者永遠同一個數。

### 起始面邊個俾換走:由數據話事,但數據要先修好
量度方法係「擲到之後有冇用」(擲出率人人一樣,冇意義)。第一次跑,刺蝟嘅
「豎棘」報 **0/3491 = 0.0%** —— 但佢係**模擬器盲點**唔係設計事實:
`_score_die` 冇一個 branch 接得住「淨係 thorns、target self」嘅面,所以政策
根本揀唔到佢。**補咗 branch 先再量。** 詳情同最終名單見 BALANCE.md。

### 解鎖面插入位置:等級表由 7 級擴到 8 級
每人加一個靈息解鎖面,插喺同級數相若嘅位置,之後全部順延一級,XP 表加 8 級
(門檻 70 —— 原本 delta 係 4/6/8/10/12/14,下一步 16)。冇任何 code 寫死級數上限,
`max_hero_level()` 本來就由表推導。

### 平衡:用「世界血量」呢把鈍刀,唔收返本輪加嘅規則
量度拆開三層(BALANCE.md 有表):
- 舊數據 + 冇 U1 + **新模擬器** → 89 / 69 / 37,**三條都入帶**。
- 加新面 → 92 / 75 / 40。
- 再加 U1 → 95 / 83 / 54。

即係話上一輪出貨嗰組數(84 / 57 / 38)有一部分係**模擬器 bug 壓低咗**,唔係設計
問題。而本輪加出嚟嘅力量真係好大 —— 主因唔係梟,係 **U2**:`base_rerolls` 係
**0**,所以「2 靈息換一次重擲」喺一隻本來冇重擲嘅遊戲入面係實打實嘅新資源。

規格點名嘅兩把刀(溢出 6→7、靈術費 +1)補唔到 10 點嘅 ch2 缺口 —— 實測溢出
6→8 只係將全通由 54 拉到 48,ch2 動都唔動(83 → 82)。所以:溢出照跟規格行
**6→7**,其餘由一個**全局敵人 HP 乘數**食,而且係**逐章**嘅
(1.10 / 1.10 / 1.03)。平嘅 1.10 會令頭兩章啱晒(87 / 67)但全通跌到 28 ——
第三章會複利(佢啲小怪本身就係 T3、boss 係全遊戲最長嗰場)。分章即係上一輪
BALANCE.md 求緊嗰條「早期強、後期唔 scale」嘅槓桿,調轉方向用。

**唔揀敵人傷害乘數**:加血會拉長戰鬥(平均回合本來跌到 4.09,已經係 4-6 帶嘅
地板),加傷害會縮短戰鬥同時打死人,係另一隻遊戲。

---

# 第七輪:導靈杖改每回合、零使用審計、資料驗證測試(2026-08-11)

## 導靈杖由「每場戰鬥一次」改成「每回合一次」
規格改動本身係指定嘅,但實作揀咗兩件事:

**一、狀態 key 改名 `first_spell_used` → `spell_cast_this_turn`。** 舊名講嘅係
「呢場仗開咗火未」,新規則問嘅係「呢個回合開咗火未」—— 名唔改嘅話,下一個讀
呢段 code 嘅人會以為佢仲係一場一次。

**二、重置點放喺 `_start_turn()`,而且刻意擺喺 `essence_reroll_used` 隔籬。**
兩者係同一種嘢:一個每回合重新發嘅額度,玩家要圍住佢計數。放埋一齊,下次再加
「每回合一次」嘅嘢就自然跟同一個模式。

**冇改嘅嘢:查詢式定價。** `spell_cost()` 仍然係「問價」而唔係「找數時先扣」,
理由同第五輪嗰條一樣(見上面「導靈杖用『查詢』而唔係『找數時先減』」)——
`can_use()`、UI 顯示、實際收費三者讀同一個 function。改成每回合之後呢點更加緊要:
同一回合第二個靈術要即刻恢復全費,顯示同收費一齊變先唔會出現「睇住 2 撳落去收 3」。

同森之心疊加(兩個 -1 相加),但 `maxi(c - x, 1)` 嘅底線唔郁 —— 2 費靈術食兩個
折扣仍然收 1。呢條寫咗入 glossary 裁定,因為佢係玩家實際會撞到嘅組合。

## 圖鑑冇遺物頁,所以「更新圖鑑」就係更新一個 key
圖鑑(`screen_codex.gd`)得兩個分頁:角色同怪物。遺物文案喺獲得卡、商店、寶箱、
戰鬥中嘅遺物列表出現,四個地方全部讀 `data/relics.json` 嘅 `desc_zh/desc_en`,
所以改一個 key 就係改晒所有畫面。冇喺任何地方複製過文案 —— 呢個係好嘢,唔使
特登去同步。

順帶記低一個真實缺口:`glossary.json` 入面八條 `ruling_*` 裁定條目(而家九條)
**冇任何畫面 surface 佢哋**。佢哋而家係設計參考資料,唔係玩家睇得到嘅嘢。新加
嘅 `ruling_channeling_rod_turn` 同樣係咁。要唔要開一個「裁定」分頁係下一輪嘅
題目,呢輪唔偷偷做。

## 零使用審計:附喺平衡報告入面,唔開新指令
第六輪發現豎棘 0/3491 係靠人肉留意到。個問題唔係「有冇工具」,係「跑平衡嗰陣
會唔會有人記得跑埋」。所以審計唔係 `--audit`,而係 `--balance` 報告最尾必然
print 出嚟嘅一段,用嘅仲要係同一批 run —— 即係話出貨數字同審計數字係同一次
量度,唔會各講各話。

**三種零要分開講**,因為佢哋要人做嘅嘢完全唔同:
- **擲到過但(近乎)冇用過** → 唔係設計弱,就係 `_score_die` 冇 branch。呢種
  最危險,因為兩者喺報告上面生得一模一樣。
- **完全冇擲到過** → 佢根本冇入過骰。係掉落池/角色池問題,唔係設計問題。
  所以按持有人分組列出,唔逐條列。
- **冇 run 執到嗰件遺物** → 同上,不過高一層。

閾值 0.5%:低過呢個數,喺 450 run 嘅樣本入面即係「幾乎冇發生過」,同零一樣
要解釋。上限唔設高過呢個,係因為再高就會將「弱」同「玩唔到」溝埋。

**審計唔會令 build 紅。** 佢係一張要人喺 BALANCE.md 逐條解釋嘅單。會紅嗰半
係新加嘅 `tests/data_policy_test.gd`。

## 補咗兩個政策盲點,沿用一個(而且要寫低理由)
審計第一次跑就捉到兩個同豎棘一模一樣嘅洞:
- **靜滯場**(`weaken` + `aoe`,target `none`,R 級共用池):`_score_die` 嘅
  `"none"` 分支成條 elif 鏈冇一格接得住淨係 weaken 嘅面。
- **竊骰**(`steal_die`,target `enemy_die`):嗰個分支淨係識 `stun`。

兩個都**補 branch 唔係申報盲點**,跟第六輪豎棘嗰條先例:「補咗 branch 先再量」。
竊骰嗰格順手隔走 boss 預告(`die < 0`)—— `_do_die_theft` 本身拒收預告,唔隔嘅話
政策會揀一個實會失敗嘅目標。

**混沌**(`wild`)保留做申報盲點,寫喺 `SimRunner.POLICY_BLIND` 連理由:佢要
「複製枱面上邊粒骰」,而複製品唔繼承原骰嘅位置加成,即係要由複製者嗰個位重新
評分全隊每一粒骰 —— 係第二套政策,唔係一個 branch。求其寫個爛政策同冇政策一樣
會低估佢,所以寧願講明「呢個數唔包混沌」。

## 資料驗證測試:靜態同實測分工,兩邊都唔可以自己講自己啱
`tests/data_policy_test.gd` 五組檢查,分工係:

1. **每個面都揀得起** —— 逐個 face 對返自己 target type 嗰行 `POLICY_KEYS`,
   一個 key 都撞唔到就紅,除非佢喺 `POLICY_BLIND` 有名有理由。
2. **`POLICY_KEYS` 唔可以係舊嘅** —— 佢係人手寫嘅 `_score_die` 鏡像,鏡會鏽。
   所以**兩個方向都查**:表入面有但 source 冇提 → 紅;source 有 branch 但表冇
   declare → 紅。第二個方向捉到 `aoe` / `pain`,兩個都係「收窄一個已經入咗嘅
   branch」而唔係「開一個新 branch」,所以另開 `POLICY_QUALIFIERS` 收佢哋 ——
   一個面淨係得 `aoe` 仍然係揀唔起嘅。
3. **豁免清單唔准發霉** —— `POLICY_BLIND` 入面嘅面必須仲存在、必須真係仲揀唔起
   (補咗 branch 就要刪走個豁免)、理由必須長過一句敷衍。
4. **面上嘅 key 全部要係詞彙表識得嘅字** —— 唔係就係打錯字,或者係一個玩家
   永遠唔會被告知嘅機制。
5. **每件遺物嘅 `effect` 至少有人讀** —— 冇人讀即係件遺物乜都唔做。

第 5 條第一次寫嗰陣淨係掃 `scripts/core/`,結果告 N13 森林圖同 N14 鐵匠信物
「乜都唔做」。實情係佢哋由 `screen_map.gd` 同 `screen_shop.gd` 讀 —— 即係話
**檢查範圍太窄嘅測試,同佢想捉嗰個 bug 一樣危險**:佢會叫你去修一啲冇壞嘅嘢。
改成掃成個 `scripts/`。

負向驗證(唔係靠信):暫時喺 `POLICY_KEYS["none"]` 刪走 `"thorns"`,測試即刻
紅返第六輪豎棘嗰兩條面,連 `_score_die` 有 branch 但表冇 declare 都一併報。
之後還原。

## 新文案帶咗五個新字,字體要重砌
`font_coverage_test` 紅咗:付、款、灰、竊、信 —— 三個嚟自新嘅 glossary 裁定,
兩個嚟自新寫嘅註解。跑返 `python tools/font_build.py` 重砌 subset(1104 → 1109
字),再 `--import` 一次。**唔改文案去遷就字體** —— 個測試嘅存在意義就係逼你
重砌,唔係逼你唔准寫字。

## 報告唔准同自己嗌交:n ≥ 300 之後唔再叫人「rerun at n=300」
`BAND_EDGE_MARGIN` 係一條固定 3 點嘅規則,同 n 冇關,所以喺 `--balance 300`
嘅輸出入面佢會叫你「rerun at n=300」—— 你啱啱就係咁跑。加咗 `ACCEPT_N_FULL`
(=300):到咗呢個 size,貼邊讀數會照直講「at the resolution floor」,唔再開一張
你已經填咗嘅單。

呢個唔係文案問題。一句叫你重跑嘅提示,喺你已經重跑咗之後仲喺度,會令人以為
「個數未定」,而事實係**個數已經定咗,只係佢真係貼住條線**。兩者要人做嘅嘢
完全相反(一個係再跑,一個係決定改唔改設計)。

## 導靈杖:數入晒帶 ≠ 件遺物修好咗
四項驗收全部入帶,但**唔可以讀成「導靈杖修好咗」**。上一輪量到佢 +0.022 全池
最低,量度工具係 `--commons`(強制授予、量 Δ 章數),唔係驗收矩陣。本輪跑咗
`--commons 120` 驗證,結果同解讀寫喺 BALANCE.md 第七輪第 5 節。

量出嚟嘅答案:**+0.022 → +0.040,由「唯一最低」變成「同底層一齊」,冇修好。**
成因係上一輪估錯咗樽頸 —— 唔係折扣頻率,係施法密度:參考隊一場仗總共先施法
1.2 次,所以「每回合一次」相對「每場一次」幾乎冇多咗機會兌現。詳細見 BALANCE.md
第七輪第 5 節。

呢條寫低,係因為「驗收入帶」係本輪最容易被誤讀嘅一句嘢:入帶只證明**冇撐爆**,
唔證明**有得益**。

順帶第二個教訓:同一份度量,`--commons 120` 排 10/14、`--commons 400` 排 13/14。
**要排名就用 400**,120 連 Δ章數都靠唔住。

---

# 第八輪:孤兒面處置、模擬器已升級模式、每輪工作規矩(2026-08-11)

## 兩個孤兒面:兩個都刪,但係兩個唔同理由

先查來歷(`git log -S`),答案好乾脆:兩個面喺 `fdb4c20`(2026-08-07)嗰陣**都係
起始面** ——

- `bdgb_guard4` 格擋4 = 獾 B 骰起始第 6 格;
- `boar_rampage` 暴走 = 野豬 A 骰起始第 6 格。

第六輪「每人加一個靈息面」就係**攞呢兩格嚟換**(獾換成靈甲、野豬換成以血引靈),
而且係**照數據揀**嘅(BALANCE.md 第六輪第 5 節:獾 2.2%、野豬 3.8%,兩個都係
該角色最少用嗰個起始面)。換完之後,兩條 entry 冇喺 `faces.json` 刪走,就變成
孤兒。**即係話:呢兩個面唔係「未接駁」,係「已經被數據判過死刑,但屍體冇搬走」。**

按規則處置:

**`bdgb_guard4`(block 4,B 骰,target self)→ 刪。** 機制上同擁有者現有面完全
重覆:獾 B 骰已經有兩個 `bdgb_guard3`(block 3),A 骰有 `bdg_guard2`,7 級仲會
解鎖 `bdg_guard4thorn`(block 4 + 荊棘 1)。佢係一個純數值中間格,冇任何新機制。

**`boar_rampage` 暴走(`self_atk_now` 2,自損 1)→ 都係刪,但佢唔係重覆。**
佢係全遊戲**唯一**一個 `self_atk_now` 面,規則上獨一無二 —— 但佢對擁有者
**結構上冇效果**:`self_atk_now` 只加使用者自己**本回合之後**嘅攻擊,而一個英雄
一回合淨係出一次手。用咗呢粒骰,佢就冇攻擊可以食到個加成。

計埋數:就算有雙月徽記(一個英雄用兩粒骰),暴走 + 猛擊4 = 6 傷 2 自損,而
猛擊4 + 蠻擊3 = 7 傷 1 自損 —— **食咗徽記都仲係嚴格更差。** 所以佢唔屬於
「有獨立機制價值」嗰一類;佢係一個陷阱面。接返入 XP 池等於喺升級嗰刻派一個
陷阱俾玩家,而且係派返一個上一輪已經量過最少人用嘅面。

第六輪其實已經寫低咗呢件事(「呢個係一個真嘅設計問題,唔喺本輪範圍,記低」)。
本輪就係嚟收呢張單。

**留低嘅嘢:`self_atk_now` 呢個 keyword 本身唔刪** —— 引擎、`_score_die`、
詞彙表全部照舊接住(同第六輪保留 `reroll_plus` / `reroll_carry` 同一個理由:
唔使錢,而且係將來有面用得返嘅接口)。`keywords_test` 嗰個測試改成用**測試專用
fixture face**(`_fx_rampage`,只喺測試 runtime 注入 `GameData.faces`),唔再靠
roster 入面一個面撐住 —— 靠 roster 撐測試,正正就係呢兩個孤兒可以喺度企兩輪
嘅原因之一。

**留返俾你決定嘅嘢:如果想保住「暴走」呢個機制,佢嘅屋企唔係野豬,係一個
出得到第二次手嘅角色**(狐狸有雙舞)。呢個係 roster 設計題,唔係清理題,所以
本輪冇做。

## 新測試:每個面必須至少有一條獲取途徑
`data_policy_test` 加第 1b 組。四條途徑:起始六格(任一粒骰)、XP 解鎖表、
共用掉落池(冇 `hero`、稀有度 C/R/E)、事件點名。一個都冇 → 紅。

刻意例外用 `NO_PATH_OK` 白名單 + 理由,而且白名單自己都受查(面唔存在要刪、
變返攞得到要刪、理由太短要紅)。**而家只有一條**:`blank` —— 佢係 boss 弄白
一格之後**寫落骰面**嘅佔位符,唔係戰利品,俾佢一條獲取途徑先至係 bug。

負向驗證:加一個假面 `_probe_face` 落 `faces.json` 而唔接任何池,測試即刻紅並
指名道姓。之後還原。

呢條檢查如果第六輪就存在,兩個孤兒喺當日就會紅,唔使等到第七輪嘅審計去撞返。

## 模擬器「已升級模式」:一個 function,兩個 caller
`gen_offers` 第四個參數(`unlocked_by_hero`)長期由模擬器傳 `{}`,所以**U 級面
由頭到尾冇入過任何一次模擬**。修法唔係喺模擬器度重寫一次解鎖規則 —— 規則搬咗
入 `GameData.unlocked_faces_at(id, level)`,遊戲(`Game.unlocked_faces()`)同
模擬器(`SimRunner._unlocked_map()`)**叫同一個 function**。

點解要咁樣:一個「用自己一套解鎖規則」嘅模擬器,量緊嘅係一隻冇人玩嘅遊戲。
`opts.hero_level` 令全隊由指定等級開 run,1 級(預設)行為同以前**完全一樣**,
所以舊數字仍然可比。

零使用審計順帶加咗第三個桶:**「等級未解鎖」**。以前所有 U 面都跌落「in scope
但冇擲到」,老實但冇用(答案永遠係「呢個 harness 冇量過佢哋」)。而家審計會
報返佢跑喺邊個等級,未解鎖嘅單獨計數 —— 剩返喺 in scope 嗰啲先係真係解釋唔到。

## 工作規矩(2026-08-11 起,每輪自動執行)
1. **每輪結尾一定 commit。** 唔會再有一輪嘅嘢淨係留喺工作樹 —— 第七輪先發現
   第六輪成套嘢冇 commit 過,結果兩輪要迫埋一個 commit。
2. **涉及 web 嘅輪(即係任何會入 `docs/index.pck` 嘅嘢:data/、scripts/、
   assets/、project.godot、export presets)**:重出 web build、`docs/` 單獨一個
   commit、push,然後**真開 live URL 驗證**。
3. 驗證要**客觀**:比對 live `index.pck` 嘅 `Content-Length` 同本地檔案,再落載
   落嚟搵一句只有新版先有嘅字串;Pages 通常慢 1–3 分鐘,要 poll,唔好見到 200
   就當贏。之後再開瀏覽器睇 console 有冇真錯誤。

呢三條同時寫咗入 Claude 嘅長期記憶(`dice-grove-round-workflow`),所以下一輪
唔使你再講一次。

## 等級對照嘅判詞要分方向,唔可以淨係報 max−min
第一版 `print_level_compare` 報一個「全通率極差」就落判詞,結果真數據跑出嚟係
6.0pt 超標,佢就叫「POWER CREEP」—— 但實情係最好嗰個等級只高過基準 2.0pt,
最差嗰個低 4.0pt。**「升級令你變強」同「升級令你變弱」係兩個相反嘅設計問題,
修法都相反,而一個 max−min 數字將兩者報成同一件事。**

而家判詞對住 **L1 基準**分開報 creep(最好等級高幾多)同 drop(最差等級低幾多),
三個分支各自有自己嘅講法。三個分支都實際行過先入(power creep 同 net downgrade
用細 n 撞出嚟,第三個分支由真數據行到)。

呢個唔係搬龍門:門檻 ≤5pt 照報、極差照報,只係**多報一個方向**。判斷仍然係
「超標」,只不過超標嘅係倒轉嗰邊。

## 刪面之前要諗存檔:加 SAVE_VERSION 4
`hero_face()` 直接 `GameData.faces[id]`,冇 fallback,所以刪一個仲喺人哋存檔入面
嘅面 id = 擲到嗰粒骰就 runtime error。而存檔喺 web 係 IndexedDB,冇人幫到手。

呢兩個面喺 8 月 8 日到 8 月 11 日 live 嗰個版本入面**正正係起始面**,所以唔係
理論風險。加 `_reseat_deleted_faces()`:資料庫唔識嘅 id 坐返該英雄嗰格嘅起始面,
玩家贏返嚟嘅共用池面唔郁。寫成**通用掃描**(唔係 hard-code 嗰兩個 id),所以
下次刪面唔使再寫,亦都唔會有人漏寫。

**規矩記低:刪任何 face / relic / hero id 之前,先問「live 嗰個版本有冇人揸住佢」。**

# 第九輪:收 offer 政策做成鏡片,唔做成政策(2026-08-11)

## 點解係 `--accept=` 修飾符,唔係另一個 `--` 指令
第八輪留低一條未答嘅問題:L8 比 L1 低 4 點,係設計問題定係「貪婪政策收面淨係
比稀有度」呢個已知盲點嘅放大?要答佢就要換收 offer 嘅規則再跑 —— 而嗰個改動
會郁到本文件每一個歷史數字。

所以佢做成**修飾符**,唔係做成新政策:`rarity` 仍然係預設同出貨 gate,`score`
要打出嚟先有,而且每份報告都會印返自己跑喺邊個 mode 上面。同一個 binary 入面
兩條路並存,先至可以喺同一批 seed 上面**逐格對照**,亦都先至證得到重構冇整爛
舊路(L1/L5/L8 三行同第八輪逐格對得上)。

`--commons` / `--levels` 都係同一個形狀:量度工具唔可以順手改咗被量嘅嘢。

## 鏡片嘅閘門要問返 `_score_die`,唔可以自己另起爐灶
`_face_points` 第一件事係問「呢個面喺 `POLICY_KEYS` 入面配唔配到 branch」——
即係 `tests/data_policy_test.gd` 問嗰條同一問題,讀同一張表。配唔到 = 0 分。

因為**成個假設就係呢件事**:貪婪政策玩唔起嘅面,對佢嚟講值零,而 `rarity`
睇唔到呢件事(U = R = 3)。如果閘門自己另有一套判斷,實驗就唔係喺度驗證假設,
而係喺度驗證我寫嗰套判斷。

## 常數要喺跑之前凍結,同埋要寫明佢凍結咗
成個結論由 `_face_points` 嘅十幾個常數決定:估貴啲 build 型面,鏡片就收佢哋,
L8 跌幅就唔會消失;估平啲就相反。**即係話「調到答案啱睇為止」係一個真係做得
到、而且事後睇唔出嚟嘅動作。** 所以常數凍結喺跑實驗之前,而且喺 BALANCE.md
同 source 兩邊都寫明呢件事 —— 寫明係唯一令佢可被質疑嘅方法。

唯一一次改動係跑之前新測試爆紅逼出嚟(孤注計到 0 分),兩個修正都係「模型講
唔通」而唔係「答案唔啱睇」,而且照寫入記錄。

## 新測試嘅理由:第二塊鏡會爛,而且爛得更靜
`POLICY_KEYS` 爛咗會令一個面玩唔到,零使用審計捉到。`_face_points` 爛咗更陰:
佢冇聽過嘅 keyword 靜靜哋計 0 分,揸住嗰個 keyword 嘅面就變成「鏡片永遠拒絕,
又冇任何一行 output 講過」。所以規則係:每個 key 要麼有價,要麼喺
`LENS_UNPRICED` 有理由;而且凡係 `_score_die` 玩得起嘅面,價一定 > 0。

## n=150 讀到嘅嘢,要 n=300 覆核先落判詞
本輪最反直覺嗰個發現唔係鏡片做到啲乜,而係:**第八輪嗰個「L8 低 4 點」,喺
n=300 之下自己縮到 1 點。** 同一件事第六輪已經發生過(42% @ n=150 → 37% @
n=300),而第八輪冇覆核就寫低咗個 4 點,再由嗰個 4 點推出一整段機制解釋。

規矩記低:**任何一個會用嚟推設計結論嘅等級/對照差值,喺 n=150 讀完之後要喺
n=300 覆核先算數。** `print_matrix` 已經對「驗收 band 邊緣」自動出呢句提醒;
對照實驗嘅差值冇人幫你出,要自己記得。

# ── 第十輪(2026-08-12):真人試玩五項修正 ──

## 任務1:敵方防禦面「即時生效」嘅界線點劃
- **診斷確認**:敵方 block 喺敵方結算階段先加、玩家攻擊全部喺之前、回合尾 `e.block = 0` 清零,而荊棘/中毒/灼燒本身無視格擋 —— 所以敵方格擋由出世到而家一點傷害都未擋過。**反擊架式(B2)同樣係死 code**:counter 喺敵方階段先 set、結算即清,玩家攻擊永遠掂唔到佢。呢輪一齊救返。
- **即時面分類器**(`_enemy_face_instant`):帶 block/charge/counter/howl 任一、而且完全冇行動 key(atk/heal/poison/burn/weaken/bind/curse/expose/mana_drain/summon/cancel_die)先算。而家全部敵面都係純面;將來如果出「攻擊+格擋」混合面,整面留返結算階段 —— 寧願格擋半邊冇用,都唔好令攻擊提早結算。
- **嚎叫都入即時名單**:佢係增益(狼群 +atk),即時生效令同回合所有狼嘅撕咬一致食到 buff(舊制係執行順序決定邊隻食到),預告卡數字亦即時正確。
- **已生效面唔可以再被暈眩/奪骰**:效果已經掛咗喺敵人身上,冇嘢好取消。呢個係玩家可鑽研嘅新現實:暈眩要留返俾攻擊/詛咒類意圖。UI 上即時面保持本色 + ✓ 標記,灰化留返俾被取消/已執行。
- **弱化 vs 即時格擋**:即時面喺玩家有機會落弱化之前已結算,所以弱化唔再影響已掛嘅格擋(敵方弱化喺回合尾清零,roll 時必為 0,無 retroactive 問題)。弱化照舊影響攻擊/治療。
- **測試結構修正**:keywords_test 補咗 14 個敵方側測試(格擋即時/回合尾清/反擊/蓄力/嚎叫/治療留結算/即時面不可 stun/穿刺過玩家格擋/攻擊 riders/弱化/易傷/全體/靈息汲取/消骰)。敵方格擋 bug 證明咗 keyword 測試淨驗玩家側係盲點。

## 任務3:起始資金 50 嘅落點
- `balance.json` 加 `start_gold: 50`,`RunState.new_run` 讀。50 = 第一章商店藥水(35)買得起、普通面(45)啱啱掂到、遺物(130)明顯未夠 —— 「買唔買」係決策,唔係「肯定買唔起」。sim 經 `new_run` 自動繼承,唔使另外教。

## 任務2:勝利畫面固定結構
- 結構定為:結算摘要(固定高度)→ 戰利品三選一 → 底部角色列(改 bottom-pinned,唔再係由頂數落嚟嘅 1000 —— 手機短 canvas 下舊做法會叠正落 footer)。
- 升級訊息 ≤2 行照舊逐行;>2 行摺疊成一行「★ N 位英雄升級了 — 點開查看」開彈窗。真人試玩嘅事故就係四行 levelled-up 將戰利品推出畫面。
- 通用規則落實為 `UIKit.scroll_screen()`:內容區高過可視高度就直向捲動。逐個畫面檢查結果:**勝利(reward/victory)同事件**今輪包咗;**圖鑑/換面/遺物詳情/商店/隊伍**本身已有 scroll,唔使郁。
- 390×664 最壞情況(4 升級 + 3 戰利品)mockup 實測:唔使捲已經全部完整可見;Playwright case 照加(`?boot=reward` 擺盤)。

## 任務4:地圖手機走位方案
- **揀咗「捲動 + 逐節點注釋」,唔使圖例條**:地圖入 ScrollContainer,九行梯子保持經典 1130→154 間距(唔再壓縮),當前可去節點自動捲到視野中間。20px 帶 outline 嘅類型名(戰鬥/精英/事件…跟語言模式)喺 390 寬 mockup 實測讀得清楚,所以唔需要後備嘅「頂部圖例條」方案。雙語模式下相鄰節點嘅注釋會有少少貼近,可讀性未受損,暫時接受。
- **未去到/已過嘅節點改為受撳**(以前 disabled):一撳彈類型名細牌 1.6 秒,係細螢幕下注釋唔夠時嘅後備通道。望遠鏡遺物嘅長按敵人預覽照舊疊加。
- **順手修咗一個潛藏 bug**:節點以前用 inset-aware 座標、路徑/樹用固定座標,一有 notch 兩者就對唔上;而家統一行 graph 內容座標,scroll 負責手機高度。

## 任務5:鎖定殘留嘅完整 audit 同重設計
釘骰(reroll lock)機制從未接上 UI —— `toggle_lock` 全 codebase 只有模擬器叫,真人玩家根本釘唔到骰。即係話:蓄力永遠 +0、呼應永遠唔成立、絕影完全用唔到、換位/雙舞係死面 —— 而 sim 一直用緊呢啲機制量平衡(sim 同真人玩緊兩隻唔同嘅遊戲)。今輪連引擎一齊清走,全部翻譯到「現有雙骰現實」:
- **呼應 Echo(已定案)**:讀同英雄另一顆骰「本回合擲出」嘅面(預設攻擊類,面上 `resonate_cat` 照舊)。孖生骰顯示乜係玩家睇得到嘅資訊;狐狸=孖骰協同定位不變。絕影(呼應條件)同步:另一顆骰擲出攻擊面先用得。
- **束縛 Bind(已定案)**:目標英雄下回合隨機一顆骰不可用(粒骰照擲照顯示 —— 見到自己失去咗乜),可被淨化移除;唔再封重擲。與偷骰嘅區別寫入 glossary:偷骰係 boss gimmick 拎走骰再歸還,束縛係普通 debuff 淨化就解。E06 纏繞面、長按敵人預告同步。
- **蓄力 Charge(audit 發現,重設計)**:「回合結束時此面仍然擲出喺骰上而未使用 → 蓄一層(上限3);使用時每層 +X;層數跟面唔跟擲(重擲唔會洗走),使用後歸零」。保留「引弓不發」嘅耐性幻想,決策=而家細細哋放定係揸住另一顆骰等佢大;每回合一動本來就要留一顆骰唔用,蓄力面而家就係嗰顆骰嘅意義。影響 HARE 5 面 + 共用池 1 面。
- **換位 Switch(audit 發現,重設計)**:舊效果「鎖定中嘅骰下回合+X」冇對應物。改為「格擋2 + 呼應:另一顆骰係攻擊面則+2」—— 用現有 resonate 機制,零新規則,而且同狐狸成套孖骰身份一致。`lock_boost` keyword 連 glossary/引擎一齊退役。
- **雙舞 Twin Dance(audit 發現,簡化)**:「使用後本回合可以再使用另一顆骰」,唔再要求另一顆骰釘住。同雙月徽記疊加嘅裁定改寫(舞完兩顆、徽記仲可以俾第二個英雄用兩顆)。
- **其他清走嘅殘留**:glossary「鎖定」條目刪除;「每回合一次」「靈息重擲」措辭去鎖;賭徒之骨 vs 蓄力裁定改寫(層數跟面,重擲唔洗走);教學文案「另一顆即時鎖住」改「本回合就不能再用」;孤兒 string `ui_die_locked` 刪除;padlock glyph 留返俾 locked-out badge。
- **Sim 誠實化**:`_manage_locks`(釘骰政策)退役;重擲政策唔再用「臨時釘住好面」呢招真人做唔到嘅嘢,改為「枱面有好骰未用就先行動、行完先擲」—— 同真人保面嘅方法一致。蓄力遙測改讀 per-slot 層數,「中途棄置」指標冇咗(新機制冇呢回事)。

## 同權守衛:政策層同 UI 釘死喺同一個行動面(2026-08-12,第十輪補遺)
- **點解要有**:第十輪發現 `toggle_lock` 喺引擎住咗四輪、UI 從未接線、唯一調用者係模擬器 —— BALANCE.md 一直度量緊一隊識做真人做唔到嘅動作嘅隊伍。呢類分歧唔可以靠下次好彩發現。
- **做法**:`BattleCore.PLAYER_ACTIONS`(10 個改狀態行動)+ `PLAYER_QUERIES`(27 個只讀查詢)係正式嘅「玩家做得到嘅事」定義;`tests/api_parity_test.gd` 逐行掃 `sim_runner.gd` 同 `screen_battle.gd`,名單以外嘅 `bc.<method>(` 調用即紅,直接寫 `bc.s`(`=`/`+=`…)即紅;讀 state 允許(UI 顯示都係咁讀)。名單自己都受驗:唔准收留 underscore 私有名、逐個名要真係 BattleCore 嘅 func、行動/查詢唔准重疊。
- **為咗令名單誠實,四個跨界私有查詢升做公開名**:`_enemy_face_value`→`enemy_face_value`、`_alive_enemies`→`alive_enemies`、`_alive_heroes`→`alive_heroes`、`_targetable_dice`→`targetable_dice`(純 rename,行為零改動)。
- **負向驗證做咗兩層**:(1) 套件內置 fixture —— 掃描器對住摻咗 `bc.toggle_lock(0,0)` 同 `bc.s.mana += 5` 嘅樣本必須每句捉到、對乾淨樣本(白名單調用+state 讀+註釋)必須零誤報,守衛壞咗自己都會紅;(2) 真身演練 —— 臨時將呢兩句插入 `sim_runner.play_battle`,套件即紅並點名行號(line 768/769),移除後還原全綠。
- **邊界**:tests/ 目錄刻意唔受管 —— 測試本來就要搣引擎內臟(craft rolls、直寫 state)先驗到規則。

# 第十一輪:大作感打磨(2026-08-17)

## 授權紅線點守
- **SFX 兩族制**:物理系(擲骰/打擊/金幣/揭卡/寶箱/UI/腳步)用 Kenney 四個 pack(全 CC0 1.0,zip URL 釘死喺 `tools/sfx_build.py`,cache 喺 tools/_sfx_cache/ 唔入 git);魔法系(治療/靈息/施法/毒/灼/升級)搵唔到貼切嘅現成聲,全部程序合成 —— 同 BGM 同一調色盤,反而令「物理=foley、魔法=合成」成為一條可讀嘅美學規則。逐項記入 CREDITS.md。
- **BGM 零外來樣本**:試過 OpenGameArt 方向,但 CC0 音樂質素/氛圍對口嘅唔穩陣,而且逐首驗授權係持續負擔。決定成個配樂用 `tools/music_build.py` 程序作曲(numpy+soundfile→OGG):Karplus-Strong 撥弦做主音、detuned pad、FFT 上色風底、合成 kick/hat。七軌 2.7MB,遠低過 10MB budget。**Loop 無縫係構造出嚟嘅**:每軌事件 render 兩次取第二輪,結尾嘅衰減尾巴天然已經響喺開頭。
- **libsndfile 陷阱(記低費事再踩)**:`sf.write()` 一次過寫幾分鐘 stereo vorbis 會 native crash(exit 127 無 traceback);逐秒 block write 就穩定。

## 混音紀律
- Master 下開 Music/SFX 兩條 bus(code 建,唔用 .tres —— export 漏咗 layout 檔會靜默 fallback 返 Master,滑桿變冇掣)。音量設定直接寫 bus;舊存檔單一 volume 遷移做兩個新 knob。
- Win/lose stinger 用 BGM 調色盤作,但掛喺 SFX bus 出:Music bus 嗰陣正忙住 fade 舊 track,stinger 唔可以陪佢一齊瀡走。每場戰勝 stinger 用 `Music.duck(-9dB)` 壓低 BGM 再回升;戰內敗北唔出 stinger(gameover 畫面出),避免兩支 6 秒 stinger 疊埋一齊變漿糊。
- Boss 戰音樂唔喺 goto 切,由 boss 登場演出自己 crossfade —— 黑幕下章節曲繼續行,橫幅落地一刻先轉,呢個時序係演出嘅一部分。

## 轉場策略
- 全部 screen 切換行「方向性 wipe 遮罩」:前進右入、返主選單左出、入戰鬥頂部直落;蓋住(0.10-0.13s)→ 背後換屏 → 反向揭開。**133ms 建屏 hitch 係 load 成本唔係 render 成本(第九輪已量),醫唔好就藏**:靜態遮罩下嘅長 frame 對眼睛係隱形嘅。
- 轉場中途再嚟嘅 goto 用 pending retarget,唔會兩個轉場打架;headless(16 suites)同首屏照舊即時同步換,測試零改動。

## 勝利畫面「XP 條動畫」點取捨
規格寫「XP條動畫填充」。Meta XP 係逐英雄一條(四~六條),結算卡上塞六條 bar 會迫爆第十輪先修好嘅固定結構;改用「金幣數字由 0 滾上去 + XP chip 彈入 + 升級有聲有粒子」承載同一個「數字喺你眼前埋單」嘅感覺。如果之後想要真 bar,應該做喺 metaprogress 頁(嗰度本來就有空間)。

## 敗北統計嘅資料通道
`on_battle_finished` 敗北路線以前 `clear_run()` 先過 `goto("gameover")`,搞到敗北畫面讀唔返章節(永遠顯示第 1 章 —— 潛藏 bug)。而家戰報(章節/戰鬥/節點)喺 wipe 之前 duplicate 咗經 goto args 帶過去,純 presentation 通道,冇嘢會讀返佢寫嘢。

## 效能:hitch 嘅兩個身位,一藏一斬,剩債記帳
第九輪量到嘅 133ms worst frame 今輪拆開睇原來係兩樣嘢:
1. **Screen 轉場嗰下嘅建屏成本** —— 唔醫,**藏**:wipe 遮罩全遮之下先換屏,
   靜態畫面下一個長 frame 對眼睛係隱形嘅(見「轉場策略」)。
2. **戰鬥內每一拍敵人行動嘅全量 declarative refresh** —— 真 GPU headed 實測
   每拍 83-133ms,元兇係每次 refresh 重建晒 8 個 Die3D(每個一個 own_world_3d
   嘅 SubViewport)。**Die3D 池化**(骰常駐、refresh 淨係 reparent+set_die)
   將佢斬半:worst 133→67ms,fps 56.7→58.5。
剩低嘅 50-67ms 係逐拍重建卡片/文字 shaping 嘅擴散成本,冇單一大件。真正解法
係 diff-based refresh(只更新變咗嘅 rows),但嗰個係全 project 最複雜 screen
嘅結構重構,唔應該喺打磨輪尾段冒呢個險 —— **記債**,perf spec
(`round11perf.spec.js`)鎖住 58.5fps/67ms/7 條線,邊個回退邊個現形。
順帶:perf 一定要 headed + 清走共用 config 嗰句 `--use-angle=swiftshader`,
唔係量出嚟係 SwiftShader 嘅 29fps,同遊戲無關。

# 第十四輪:Event 擲骰互動 + 遺物/藥水圖示(2026-08-18)

本輪純 UI/UX。零數值、零池結構改動 —— `--balance 150`(3 seed sets)前後
逐行對齊,value_band / creep / 池 suites 原封綠。

## 「誠實擲骰」點做到唔係口號

Event 嘅擲骰判定(V03 賭骰攤)以前係「撳選項 → 即刻出一句『擲出 4 —— 贏了』」。
新做法係:**引擎先擲,widget 只負責展示**,而且呢個次序係機器可驗嘅:

- `screen_event._resolve()` 嘅 RNG 調用次序**一個字都冇改**:扣注金 →
  `rng.randi_range(1,6)` → `save_rng`。個數字擲完先交畀 `DiceCheck.open()`,
  所以 sim 同存檔嘅 RNG 流完全一樣(sim 本來就唔賭,`_do_event` 嗰路唔經呢度)。
- `Die3D.throw(final_face)` 本身就係「話畀佢聽落邊面」嘅 API —— 冇「演完先抽數」
  嘅位。**驗法唔係睇 code**:`tests/round14_ui_test.gd` 逐個 1-6 揸過,由
  widget 讀返 cube 面上嗰粒數(`_shown_pip()` 讀 `die.faces[die.shown].pip`)
  同引擎個數比;`round14dice.spec.js` 喺真 browser 度用 CDP 真 touch 再驗一次,
  仲會用一條**獨立 RNG replay**(`RunState.rng_of` 係純函數)預測應該擲幾多,
  再對住個 cube。兩層都係「讀返畫面」而唔係「讀返 caller 個變數」。
- 錢包喺確認之前唔郁。贏嘅金幣係 `on_done` 先入賬 —— 玩家見到粒骰停低、
  見到判詞,撳咗確認,先至埋單。

## 點解係點數骰(pip)而唔係戰鬥骰面

「沿用戰鬥骰美術」執行成:**同一個 `Die3D`** —— 同一舊倒角立方體、同一個
atlas SubViewport、同一套 flat fill + 重墨邊、同一條 throw 曲線。差別淨係
face 內容:`{"pip": n}` 行 `Glyphs._pips`,畫傳統六面點數。
理由:運氣判定唔係一個英雄骰面。喺一個「數字決定成敗」嘅畫面,如果粒骰印住
「4」而個 4 平時代表傷害,兩個意思會撞;點數係全世界都識嘅「呢粒係運氣骰」。
呢個係**內容**上嘅分別,唔係第二套美術 —— 一行 `pip` 分支住喺 `_FaceArt`,
冇新 widget、冇新 shader、冇新 atlas。

## 手勢:抽出嚟,而唔係再寫一次

`FaceTile` 本來有一套好嘅按壓規則(0.45s 長按、24px TAP_SLOP、
`NOTIFICATION_SCROLL_BEGIN` 取消),而 `screen_battle._attach_longpress` 另外
有一個裸 timer —— 冇死區、冇 scroll 取消,喺捲動途中會彈卡。呢輪抽咗做
`PressGesture`,三方(FaceTile / ItemIcon / _attach_longpress)共用一份。
順手修咗兩個「lambda 捉住已 free 嘅 node」嘅 console error 來源:
signal 連 method callable(engine 會自動斷)而唔係連 lambda(唔會),
`Fx.burst` 嗰個 cleanup timer 亦同理改成 `p.queue_free` 直連。

## 藥水:tap 係「問」,唔係「飲」

藥水格就喺骰下面,一支藥水飲咗冇得返轉頭 —— 而且改之前**要睇效果就要長按**,
偏偏「我想知呢個係乜」嘅本能動作係 tap。而家反過嚟:tap 出「使用/取消」卡
(卡身就係效果卡),長按淨係睇。取消 = 撳卡外/✕,同全遊戲其他卡一致,冇多寫
一個 dialog。已經 armed 等目標嗰支再 tap 一下照樣係取消 —— 確認係為咗「飲」,
唔係為咗「瞄」。

## 圖示:數據驅動 + linter 守

遺物本來就喺 `relics.json` 帶住 `glyph`;藥水而家一樣(六個新圖,同遺物一套
32×32 物件語言:共用樽身 `_flask`,樽入面嘅嘢先係分別)。UI 層一律讀
`GameData` 個 `glyph`/`desc_*`,冇第二份手抄描述。
`tests/icon_coverage_test.gd`(第 19 套 Godot suite)逐個 id 驗四件事:有 `glyph`、
key 喺 `Glyphs.KEYS`、**glyphs.gd 真係有 match arm 畫佢**(淨係喺 KEYS 度掛名
會靜靜地跌落 `_unknown` 空碟)、同埋冇兩件嘢撞圖。負向驗過:抽走 P06 個 glyph
+ 畀 P05 一個唔存在嘅 key → 3 條 FAIL,exit 1。
